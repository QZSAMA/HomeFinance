name: Browser E2E

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: browser-e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  playwright-compose:
    name: Playwright against Compose
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Install Playwright Chromium
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Run Playwright E2E suite
        working-directory: frontend
        env:
          E2E_KEEP_STACK: '1'
        run: npm run test:e2e

      - name: Dump Compose diagnostics on failure
        if: failure()
        run: |
          docker compose -p homefinance-e2e -f docker-compose.e2e.yml ps || true
          docker compose -p homefinance-e2e -f docker-compose.e2e.yml logs --no-color frontend backend || true

      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts-${{ github.run_id }}
          path: |
            frontend/playwright-report
            frontend/test-results
          if-no-files-found: ignore
          retention-days: 14

      - name: Tear down E2E stack
        if: always()
        run: docker compose -p homefinance-e2e -f docker-compose.e2e.yml down --volumes --remove-orphans
