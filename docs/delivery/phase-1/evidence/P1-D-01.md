# P1-D-01 Evidence

- Task ID: P1-D-01
- Evidence status: PASS-REAL for local PostgreSQL schema, migration, uniqueness, and history retention
- Baseline commit: 582db55 on codex/phase1-ledger-trust
- Branch/commit: codex/phase1-ledger-trust@370b2d9
- Source evidence: backend/prisma/schema.prisma, migrations 20260828100200_phase1_add_recurring_execution and 20260828100210_phase1_add_recurring_soft_delete, backend/src/tests/phase1SchemaContract.test.ts, and backend/src/tests/recurring.phase1.integration.test.ts
- Affected families/roles/routes/tables: family-scoped RecurringTransaction and new RecurringExecution; an execution belongs to one family and one rule, while the occurrence identity is (recurringTransactionId, scheduledFor)
- RED command: npm test -- --runInBand src/tests/phase1SchemaContract.test.ts src/services/recurringService.phase1.test.ts src/routes/recurring.phase1.test.ts; the first run failed because the schema/migration did not declare RecurringExecution, occurrence uniqueness, or recurring tombstones
- RED failure summary: schema contract and new service/route imports failed on the missing persistence and execution boundaries; this was an expected implementation RED rather than a PostgreSQL availability failure
- GREEN scope: added additive RecurringExecution persistence with PROCESSING/COMMITTED/FAILED status, request key, ledger entry reference, mutation reference, replay result, family/rule foreign keys, family/date index, and named unique occurrence index. Added nullable RecurringTransaction.deletedAt plus family/deletion index without deleting existing rules or executions
- REFACTOR summary: deletion now tombstones and deactivates a rule instead of physically deleting it; list, due, update, and execute paths exclude tombstones so committed execution history keeps a valid rule reference
- Regression commands/results: backend focused recurring/schema tests PASS; npm run build PASS; npx prisma validate and npx prisma format --check PASS; npm run test:integration PASS, 8 suites / 47 tests; full backend coverage execution passes all 42 suites / 344 tests but the command remains FAILED because branches are 47.68% versus the unchanged 60% global threshold
- Security negative matrix: family deletion cascades executions only with the tenant aggregate; rule deletion is non-destructive. Existing route/role tests deny unauthenticated, viewer, unknown-role, and non-member mutation before the execution service; cross-family execution lookup remains family-scoped
- Database/migration evidence: PostgreSQL 18.1 local test database PASS-REAL. Fresh schema p1_recurring_fresh_20260901 applied all 9 migrations and passed all 6 recurring integration cases; that temporary schema was explicitly dropped afterward and the public schema/data was untouched
- Frontend/E2E evidence: no persistence behavior is delegated to the frontend; browser E2E NOT_RUN
- Release environment/time: local Windows/PostgreSQL 18.1, 2026-09-01; staging/release observation NOT_RUN
- Observed result: REGRESSION_VERIFIED / AT_RISK; the additive schema and fresh-migration path are locally verified, while populated upgrade/restore, E2E, staging, and release observation remain open
- Rollback rehearsal: fresh forward migration was rehearsed, but populated restore was not. Roll back the route/service entry point or forward-fix while retaining additive execution/audit facts; do not drop RecurringExecution or revive destructive rule deletion after committed executions exist
- ADR/project-memory/audit/Graphify updates: ADR-0006 records occurrence ownership and tombstone semantics; tracker, project memory, and HF-DATA-001 status updated. Semantic Graphify refresh remains pending because the installed AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: no production traffic observation, populated upgrade/restore, full Compose/E2E, or global coverage pass; execution cleanup/retention policy is intentionally not automated in this slice
