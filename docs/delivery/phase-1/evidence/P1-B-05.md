# P1-B-05 Evidence

- Task ID: P1-B-05
- Evidence status: PASS-REAL
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; P1-B-05 verification slice (no production implementation change)
- Source evidence: Phase 1 design §4.2/§9, P1-B-04 real coordinator arbitration, ADR-0001 and ADR-0002
- Affected families/roles/routes/tables: family-scoped LedgerApplicationService/FinancialMutationCoordinator, Family.cacheVersion trigger, Income, IdempotencyRecord and AuditEvent; no HTTP route behavior changed
- RED command: N/A for this regression-verification-only slice: no product behavior was implemented. `$env:RUN_INTEGRATION='1'; $env:DATABASE_URL='postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public'; npx jest src/tests/phase1-concurrency.integration.test.ts --runInBand --detectOpenHandles` was run immediately after adding the two real-PostgreSQL contracts.
- RED failure summary: The new contracts passed on their first real-PostgreSQL execution, demonstrating existing trigger/coordinator behavior rather than exposing a product defect. The first expanded focused run did fail only because two pre-existing suite-wide assertions used absolute Income counts; they were refactored to `beforeIncomeCount + 1` after the new tests legitimately added prior rows.
- GREEN commit and scope: No production code changed. `phase1-concurrency.integration.test.ts` now proves that twenty identical concurrent `createIncome` commands advance Family.cacheVersion exactly once, and that a completed replay adds no Income, IdempotencyRecord, or AuditEvent and does not advance the family revision.
- REFACTOR summary: Replaced the shared-family suite's brittle hard-coded Income counts in the different-payload and response-loss cases with relative counts. Assertions retain the original one-write contract while remaining independent of test ordering and preceding fixtures.
- Regression commands/results: focused real PostgreSQL concurrency suite PASS, 1 suite / 7 tests; `npm run test:integration` PASS, 3 suites / 27 tests; `npm run build` PASS; `npx prisma validate` PASS; `npx prisma format --check` PASS. Required `npm test -- --runInBand --coverage` executed 34 suites / 281 tests with statements 63.22%, branches 46.01%, functions 64.06%, and lines 63.58%; it correctly fails only because the unchanged global branch threshold is 60%. Existing OCR/MinIO failure-fixture console warnings remain.
- Security negative matrix: The assertions exercise the family-scoped mutation coordinator and its database arbitration, but do not add HTTP unauthenticated/non-member/viewer ingress coverage. Existing P1-B-04 proof retains authorization before root winner lookup; route adoption remains mandatory before claiming route-level exactly-once behavior.
- Database/migration evidence: PASS-REAL on dedicated local PostgreSQL 18.1 at localhost:5433, database `homefinance_phase1_test`. Twenty concurrent identical commands produce one committed Income and advance the durable Family.cacheVersion by one; replay of a completed command leaves all three persisted fact counts and Family.cacheVersion unchanged.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: local Windows, PostgreSQL 18.1 dedicated non-production database, 2026-08-31; staging/release observation not performed
- Observed result: Existing PostgreSQL trigger/coordinator behavior satisfies this local revision/replay contract: one committed mutation advances the revision once and a completed identical replay is read-only. This is PASS-REAL regression verification, not route adoption or a new production capability.
- Rollback rehearsal: No migration or production code changed. Test-only rollback is `git revert` of this verification commit; existing additive migration and forward-fix policy remains unchanged.
- ADR/project-memory/audit/Graphify updates: project memory and tracker record the local PASS-REAL scope; semantic Graphify incremental update is required for this test/documentation change. Only reviewed source-backed P1-B-05 edges may be treated as EXTRACTED.
- Remaining risks: Income/Expense routes still bypass the coordinator; import/recurring/AI mutation adoption, populated upgrade/restore, Redis/Compose, staging and release observation remain unverified. P1-B-04's historical unclassified aggregate-run INTERNAL_ERROR remains AT_RISK. The global coverage gate remains below the unchanged branch threshold.
