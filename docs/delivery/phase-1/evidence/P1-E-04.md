# P1-E-04 Evidence

- Task ID: P1-E-04
- Evidence status: REGRESSION_VERIFIED / AT_RISK
- Baseline commit: `7f6c366`
- Branch/commit: `codex/phase1-ledger-trust@c769a78` (implementation `cdfcb67`; frontend/history follow-up `d945e65`, `c769a78`)
- Source evidence: `backend/src/services/aiProposalConfirmationService.ts`, `backend/src/services/financialMutationCoordinator.ts`, `backend/src/routes/ai.ts`, `backend/src/services/aiProposalConfirmationService.test.ts`, `backend/src/tests/aiProposalRoutes.phase1.integration.test.ts`, ADR-0004
- Affected families/roles/routes/tables: family-scoped proposal confirmation for member/admin writers; viewer and non-member denial; `/api/families/:familyId/ai/proposals/:proposalId/confirm` and strict legacy adapter; `AiProposal`, `AiProposalItem`, Income/Expense, `IdempotencyRecord`, `AuditEvent`
- RED command: `cd backend; npm test -- --runInBand src/services/aiProposalConfirmationService.test.ts`; the initial confirmation contract failed before the coordinator, proposal claim and Ledger transaction path existed.
- RED failure summary: the pre-change confirmation path could not prove server-owned proposal identity, atomic claim-plus-ledger behavior, replay, or stable result persistence. The intended TDD regressions covered duplicate confirmation, edited actions, expiry, malformed data, and transaction failure.
- GREEN scope: `cdfcb67` routes proposal confirmation through `FinancialMutationCoordinator`; it validates family/actor context and server-owned proposal hash/version, conditionally claims `PROPOSED` to `CONFIRMING`, creates Income/Expense through the transaction-backed Ledger adapter, records item results, writes AuditEvent and idempotency response, and completes `EXECUTED` in the same transaction. Same-key replay returns the stored result; a different key cannot confirm an already executed proposal; concurrent requests are arbitrated by the database.
- Regression commands/results: backend AI route/service regression PASS, 3 suites / 52 tests; frontend AI page/service regression PASS, 2 files / 6 tests; real PostgreSQL AI proposal route integration PASS, 17 tests. The integration suite proves executed history includes persisted result metadata and a second confirmation does not create another Income. `npm run build`, Prisma validate/format and prior full backend gates pass. Global coverage remains a separate failed gate because the unchanged branch threshold is below 60%.
- Security negative matrix: real HTTP/PG evidence covers viewer, non-member, cross-family proposal, expired proposal, malformed amount/date/type, duplicate/unknown item IDs, missing idempotency key, same-key payload tamper, already executed proposal and concurrent confirmation. Negative paths leave ledger facts and idempotency/audit state unchanged where applicable. Redis was unavailable; rate-limit middleware failed open with known test noise, so Redis behavior is not claimed.
- Database/migration evidence: PASS-REAL on local PostgreSQL 18.1 at `localhost:5433`; no new migration was required by confirmation because the proposal and coordinator contracts already exist. The 17-test route suite uses the existing additive proposal schema and verifies persisted Income/result/audit/idempotency behavior.
- Frontend/E2E evidence: PASS-MOCK through React Testing Library/Vitest for confirmation retry, pending-click guard, server-owned proposal ID/item mapping, pending history recovery and executed history status. Playwright, multi-tab, browser network and Compose evidence remain NOT_RUN.
- Release environment/time: local Windows, PostgreSQL 18.1, 2026-09-01; staging/release observation NOT_RUN
- Observed result: proposal confirmation is atomic and idempotent for supported Income/Expense actions. Asset/Liability actions intentionally return `AI_BALANCE_MUTATION_UNAVAILABLE` until the Balance mutation service is adopted. The legacy `/execute-actions` compatibility surface remains an explicit closure risk.
- Rollback rehearsal: no destructive rollback is allowed for populated proposal tables. If required, disable proposal confirmation through a forward fix while preserving proposal/audit facts; full staging rollback/restart rehearsal remains open.
- ADR/project-memory/audit/Graphify updates: ADR-0004, project memory, audit risk and tracker are updated with this evidence. Semantic Graphify refresh remains pending because the available updater is AST-only and must not replace the reviewed semantic graph.
- Remaining risks: Balance mutation support, legacy raw-action endpoint removal/containment, Redis/MinIO/Compose, browser E2E, populated restore, staging/release observation and global branch coverage.
