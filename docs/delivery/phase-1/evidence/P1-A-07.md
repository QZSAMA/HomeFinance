# P1-A-07 Evidence

- Task ID: P1-A-07
- Evidence status: REGRESSION_VERIFIED / AT_RISK (AI scope only)
- Baseline commit: `081084a`
- Branch/commit: `codex/phase1-ledger-trust@6704300`
- Source evidence: `backend/src/services/aiActions.phase1.test.ts`, `backend/src/routes/ai.test.ts`, `backend/src/routes/ai.ts`, `backend/src/services/aiProposalConfirmationService.ts`
- Affected families/roles/routes/tables: family-scoped AI chat/OCR/proposal confirmation and the compatibility `/execute-actions` route; Income/Expense Ledger, AuditEvent, IdempotencyRecord and AiProposal/AiProposalItem
- RED command: `cd backend; npm test -- --runInBand src/services/aiActions.phase1.test.ts` (the new boundary test was introduced before removing the executor)
- RED failure summary: The boundary test initially failed because `aiActions` still exported the direct Prisma-backed `executeActions` financial mutation executor.
- GREEN scope: Commit `6704300` removes the direct executor and its Prisma/decimal dependencies. AI proposal confirmation remains the only financial execution service; `/execute-actions` accepts only proposal ID/version/hash/actions and delegates to that service.
- REFACTOR summary: Route and family-permission tests no longer mock or assert the deleted executor; viewer denial is asserted against the underlying Income mutation boundary.
- Regression commands/results: `cd backend; npm test -- --runInBand src/services/aiActions.phase1.test.ts src/routes/ai.test.ts src/routes/aiProposalConfirmRoute.phase1.test.ts src/tests/family-permissions.test.ts` — PASS, 4 suites / 52 tests. `npm run build` — PASS. Static `rg` confirms no production import or implementation of `executeActions`; the compatibility route name remains intentionally present.
- Security negative matrix: AI viewer/non-member/tamper/expiry/replay/concurrency evidence remains in P1-E-06 and its linked real PostgreSQL route suite. This card proves removal of the raw direct executor, not complete closure of every financial mutation path.
- Database/migration evidence: No schema change in this slice; existing proposal/confirmation PostgreSQL evidence remains linked from P1-E-06.
- Frontend/E2E evidence: Not applicable to the deleted executor; browser E2E remains blocked by unavailable Docker.
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: AI no longer exposes the old direct Prisma executor. P1-A-07 stays AT_RISK because Asset/Liability and other non-AI mutation routes still require the same global Ledger-bypass audit, and the compatibility URL is still a release risk.
- Rollback rehearsal: Forward-fix only; do not restore the direct executor. If compatibility removal causes clients to fail, migrate clients to `/ai/proposals/:proposalId/confirm`, then remove the adapter after telemetry and E2E evidence.
- ADR/project-memory/audit/Graphify updates: `docs/project-memory.md`, tracker, P1-E-06 and HF-AI-001 are synchronized; semantic Graphify refresh remains pending because the available updater is AST-only.
- Remaining risks: full Ledger adoption audit, Balance action support, compatibility adapter removal, external infrastructure, browser E2E, release observation and global branch coverage.
