# P1-E-02 Evidence

- Task ID: P1-E-02
- Evidence status: PASS-REAL for additive schema, migration, defaults, constraints and family cascade
- Baseline commit: 134e6eb on codex/phase1-ledger-trust
- Branch/commit: codex/phase1-ledger-trust@732eafd
- Source evidence: backend/prisma/schema.prisma, migration 20260828100400_phase1_add_ai_proposal, backend/src/tests/aiProposalSchemaContract.phase1.test.ts, backend/src/tests/aiProposal.phase1.integration.test.ts, ADR-0004
- Affected families/roles/routes/tables: new family-owned AIProposal/AIProposalItem tables exposed through Prisma as AiProposal/AiProposalItem; optional actor, conversation and file references preserve historical proposal facts through SetNull, while family deletion cascades the tenant aggregate
- RED command: `npm test -- --runInBand src/tests/aiProposalSchemaContract.phase1.test.ts`; then `RUN_INTEGRATION=1 DATABASE_URL=<localhost:5433 test database> npx jest src/tests/aiProposal.phase1.integration.test.ts --runInBand --forceExit --detectOpenHandles`
- RED failure summary: the static contract failed because `model AIProposal` was absent. The real PostgreSQL suite then failed with `public.AIProposal does not exist` against an existing test database containing the prior nine migrations. The Prisma API naming refactor also produced an expected RED until the models became AiProposal/AiProposalItem with `@@map` to the unchanged database table names
- GREEN commit and scope: `732eafd` adds original/confirmed JSON payloads and SHA-256 hashes, actor snapshot, source type/conversation/file, status, optimistic version, expiry, result JSON and ordered typed items. SQL enforces hash format, confirmed payload/hash pairing, version > 0, controlled source/status values, non-negative ordinal and unique `(proposalId, ordinal)`
- REFACTOR summary: Prisma model names use `AiProposal`/`AiProposalItem`, yielding readable `prisma.aiProposal` delegates while preserving the approved `AIProposal`/`AIProposalItem` physical table names. The status set is frozen in ADR-0004; actor/source FKs preserve history through SetNull
- Regression commands/results: focused schema contracts PASS, 3 suites / 5 tests; backend build PASS; Prisma generate/validate/format/status PASS. Full integration PASS, 9 suites / 50 tests. Full backend coverage executes 43 suites / 345 tests with every behavior assertion passing; statements 67.47%, branches 47.63%, functions 64.16%, lines 68.01%, and the command remains non-zero only because the unchanged 60% branch threshold is not met
- Security negative matrix: the database enforces family ownership, valid hashes/status/version and ordered item uniqueness, but it cannot express viewer permissions or same-family source relationships. P1-E-03/E-04 must authorize before proposal lookup/AI access, require current write membership, validate source conversation/file family equality and treat all payloads as untrusted
- Database/migration evidence: PASS-REAL on local PostgreSQL 18.1 at localhost:5433. The additive migration applied to the existing nine-migration `homefinance_phase1_test`, the proposal integration suite passed 3/3, and an isolated fresh database applied all ten migrations, verified both proposal tables, then was explicitly dropped. The full integration suite passed 9 suites / 50 tests
- Frontend/E2E evidence: no frontend contract changes in this persistence-only slice; E2E NOT_RUN
- Release environment/time: local Windows/PostgreSQL 18.1, 2026-09-01; staging/release observation NOT_RUN
- Observed result: REGRESSION_VERIFIED / AT_RISK. Proposal state, source, payload hashes, expiry and ordered actions are now durable and locally migration-safe; no route consumes these tables yet
- Rollback rehearsal: fresh forward deploy and cleanup passed. Do not drop proposal tables after they contain facts; disable proposal-producing entry points or forward-fix the additive schema. Production-like populated restore and staging rollback remain open
- ADR/project-memory/audit/Graphify updates: ADR-0004 is Accepted and records lifecycle/actor/source semantics; tracker, project memory, audit risk and P1-G evidence are synchronized. Semantic Graphify refresh remains pending; the installed AST-only updater must not overwrite the reviewed semantic graph
- Remaining risks: chat/OCR persistence service, canonical JSON/hash implementation, same-family source validation, payload/item size limits, proposal cleanup/retention, transaction-backed confirmation, Balance mutation orchestration, idempotent replay/concurrency, negative role/tamper/expiry matrix, frontend proposal state, E2E and release observation
- Next review: begin P1-E-03 with a focused route/service RED proving chat and OCR persist one server-owned proposal and return proposalId/version/hash/expiresAt without ledger writes
