# P1-D-03 Evidence

- Task ID: P1-D-03
- Evidence status: PASS-REAL for HTTP-to-PostgreSQL route adoption and PASS-MOCK for frontend retry identity
- Baseline commit: 582db55 on codex/phase1-ledger-trust
- Branch/commit: codex/phase1-ledger-trust@370b2d9
- Source evidence: backend/src/routes/recurring.ts, backend/src/routes/recurring.phase1.test.ts, backend/src/routes/recurring.test.ts, frontend/src/services/recurringService.ts, frontend/src/services/recurringService.test.ts, frontend/src/pages/RecurringPage.tsx, and frontend/src/pages/RecurringPage.test.tsx
- Affected families/roles/routes/tables: POST /api/families/:familyId/recurring/:id/execute for authenticated family writers; frontend uses the selected family's displayed nextDate as scheduledFor and one request key per occurrence
- RED command: npm test -- --runInBand src/routes/recurring.phase1.test.ts; the first run failed because the legacy handler directly created Income/Expense and updated RecurringTransaction instead of calling an application service. Frontend REDs failed because execute sent neither occurrence nor a stable key
- RED failure summary: route-local family access and direct Prisma ledger mutation remained; replay metadata/stable recurring errors were absent; frontend retries could not prove they referred to the same occurrence
- GREEN scope: execute now uses centralized requireFamilyWriteAccess, parses optional compatible scheduledFor, forwards actor/family/rule/key to executeRecurring, exposes executionId, entryId, operationId, deduplicated, and Idempotency-Replayed. The route no longer directly writes Income/Expense or advances a recurring rule
- Frontend scope: service always sends the displayed occurrence and Idempotency-Key; the page creates one key per ruleId + nextDate, retains it after failure for a safe retry, and removes it only after a successful execution refresh
- REFACTOR summary: removed route-local checkFamilyAccess; due/list routes use centralized read authorization; update/delete are family-scoped and exclude tombstones; response keeps the success message/nextDate/isActive fields while adding execution metadata
- Regression commands/results: focused backend recurring route/service/schema suites PASS; real recurring integration PASS, 6 tests; full integration PASS, 8 suites / 47 tests. Frontend focused recurring service/page tests PASS, 2 files / 2 tests; full frontend PASS, 6 files / 15 tests; lint PASS with 16 pre-existing warnings; build PASS with the known 857.88 kB main-chunk warning
- Security negative matrix: centralized middleware denies unauthenticated/viewer/unknown-role/non-member writers before service execution; existing recurring route tests include non-member/viewer denial; service performs family-scoped lookup and authorization-before-replay. The route rejects malformed scheduledFor through Zod
- Database/migration evidence: HTTP tombstone and execution path are included in local PostgreSQL PASS-REAL; generated entry and occurrence execution IDs are independently asserted
- Frontend/E2E evidence: PASS-MOCK for request contract and failed-retry key reuse; browser/network E2E, page reload recovery, and multi-tab behavior remain NOT_RUN
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: REGRESSION_VERIFIED / AT_RISK; backend route adoption and local database behavior are verified, while browser E2E and release observation remain open
- Rollback rehearsal: disable/revert the execute adapter and client together; preserve additive execution/audit history and do not restore direct route-level ledger writes
- ADR/project-memory/audit/Graphify updates: ADR-0006, tracker, project memory, HF-DATA-001, and P1-G-03 updated. Semantic Graphify refresh remains pending because AST-only refresh would discard reviewed semantic edges
- Remaining risks: UI key state is memory-local and not restored after a full reload; no automatic background executor exists; external object/AI/Compose paths are outside this slice
