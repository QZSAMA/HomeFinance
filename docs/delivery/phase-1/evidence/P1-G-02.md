# P1-G-02 Evidence

- Task ID: P1-G-02
- Evidence status: PASS-REAL for the complete family-scoped authorization negative matrix; positive member/admin coverage remains partial, so the task stays REFACTORED / AT_RISK
- Baseline commit: `97a3a70` on `codex/phase1-ledger-trust`
- Source evidence: `backend/src/middleware/familyAccess.ts`, `backend/src/middleware/familyAccess.test.ts`, `backend/src/routes/families.ts`, `backend/src/routes/families.test.ts`, `backend/src/tests/roleMethodMatrix.integration.test.ts`
- Affected families/roles/routes/tables: family-scoped mutation entries for Income, Expense, Asset, Liability, Budget, Goal, Recurring, File, Import, AI, and Family administration; roles are unauthenticated, non-member, viewer, member, admin, and unknown `auditor`; rejection snapshots cover Income, Expense, FamilyMember, and Family state
- RED command: `npm test -- --runInBand src/routes/families.test.ts -t "viewer removing|final administrator"`
- RED failure summary: viewer self-removal returned 200 and called `familyMember.delete`; sole-admin self-demotion returned 200 and called `familyMember.update`. A middleware focused RED also failed because the administrator-specific access exports did not exist.
- GREEN scope: family administration update/delete/invite/role routes now use centralized `requireFamilyAdminAccess`; member removal uses centralized `requireFamilyMemberRemovalAccess`, allowing only admins or a member removing themselves and denying viewers. Role changes cannot demote the final administrator. Existing family-scoped resource mutation routes continue to pass through `requireFamilyWriteAccess` before their handlers.
- REFACTOR summary: `familyAccess.ts` now shares role evaluation, membership loading, request context attachment, family parameter selection, and fail-closed error handling across `familyId` and `id` route shapes. Existing invite fixtures were updated for the additional middleware membership lookup; no route-local authorization copy was added.
- Regression commands/results: focused family authorization tests PASS, 2 suites / 14 tests. Real PostgreSQL role/method matrix PASS, 1 suite / 3 tests; it covers every registered family-scoped mutation entry with 401 for unauthenticated and 403 for viewer, unknown role, and non-member before handler side effects, plus authenticated-member/admin positive cases for ledger and family administration. Full integration PASS, 5 suites / 32 tests. Backend build PASS. Full default behavior tests PASS, 37 suites / 322 tests; coverage is statements 68.54%, branches 50.48%, functions 66.76%, lines 69.08% and correctly fails only the unchanged global branch threshold of 60% for branches.
- Security negative matrix: PASS-REAL for all registered family-scoped mutation entries: unauthenticated requests return 401; viewer, unknown role, and non-member requests return 403; the family snapshot is unchanged and no handler-side external or persistence effect is reached. PASS-MOCK/PASS-REAL positive checks cover member/admin Income/Expense and admin family administration. Positive 2xx proof for every non-ledger resource, plus full route-specific object-isolation and external dependency checks, remains open.
- Database/migration evidence: no schema or migration change. Local PostgreSQL 18.1 at `localhost:5433/homefinance_phase1_test` ran the matrix against actual JWT, Express route, membership, and persistence boundaries. The suite leaves no test family or user facts after cleanup; full PostgreSQL integration is 5 suites / 32 tests.
- Frontend/E2E evidence: no frontend code changed. Frontend lint/build remain passing with the existing 16 warnings and oversized main chunk; browser E2E remains NOT_RUN.
- Release environment/time: local Windows, 2026-09-01; no staging/release observation.
- Observed result: the known viewer self-removal and final-admin demotion paths are closed, and all registered family mutation guards have real negative-path evidence. Keep P1-G-02 REFACTORED / AT_RISK until positive member/admin coverage is complete for every applicable entry and the global coverage gate is closed.
- Rollback rehearsal: not run. The authorization change is a fail-closed middleware/route guard and final-admin validation; revert as one code entry point if required. No persisted data is deleted by rollback.
- ADR/project-memory/audit/Graphify updates: ADR-0001 remains applicable. Project memory and tracker record the real negative matrix and final-admin protection. Semantic Graphify refresh remains pending because the installed AST-only updater would overwrite the reviewed semantic graph; no degraded graph output is claimed.
- Remaining risks: positive member/admin matrix for non-ledger resources, object-level cross-family checks, AI/import/recurring transactional adoption, global branch coverage, upgrade/restore, Redis/MinIO/Compose, browser E2E, staging/release observation, and semantic Graphify tooling.
