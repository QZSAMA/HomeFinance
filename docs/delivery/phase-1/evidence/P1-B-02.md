# P1-B-02 Evidence

- Task ID: P1-B-02
- Evidence status: PASS-MOCK
- Baseline commit: ad17629
- Branch/commit: codex/phase1-ledger-trust; implementation commit `36c710d`
- Source evidence: `docs/superpowers/specs/2026-08-28-homefinance-phase1-design.md` sections 4.1, 4.2 and 9; implementation plan Task 3; ADR-0002
- Affected families/roles/routes/tables: pure `FinancialMutationCoordinator` contract for family + actor scope + operation + key; planned IdempotencyRecord/AuditEvent ports only, with no Prisma schema/migration/route changes
- RED command: from `D:\Repo\Qz_Private\HomeFinance\.worktrees\phase1-ledger-trust\backend`, `npm test -- --runInBand src/services/ledgerApplicationService.test.ts src/services/financialMutationCoordinator.test.ts`
- RED failure summary: first run failed because coordinator/types/errors modules did not exist; second cycle proved that source was missing from request hash because an IMPORT command with the same key/payload incorrectly resolved as a MANUAL replay
- GREEN commit and scope: `36c710d`; deterministic SHA-256 canonical payload hash, source-aware request hash, scoped idempotency lookup/create/result save, same-hash replay, different-hash 409, audit write and Prisma-code-to-domain-error mapping
- REFACTOR summary: coordinator depends only on `FinancialMutationStore`/`LedgerTransactionClient`; canonicalization and Prisma error mapping are pure functions; Redis/process locks and generated Prisma types are absent from the correctness contract
- Regression commands/results: focused command PASS, 2 suites / 11 tests. Full-suite/toolchain status remains blocked by the unrelated stale generated Prisma Client and one failed network attempt to generate it; no full build/coverage claim is made
- Security negative matrix: family membership and allowed role are checked inside the transaction before idempotency, ledger or audit mutation; viewer zero-side-effect evidence is in the shared focused suite. Cross-family/non-member and concurrency require later negative/integration tests
- Database/migration evidence: NOT_RUN. Sequential fake-transaction replay/conflict is PASS-MOCK; database unique arbitration, concurrent P2002 recovery, rollback and response-loss replay remain P1-B-01/P1-B-04 gates
- Frontend/E2E evidence: N/A; no route/client integration in this task
- Release environment/time: local pure TypeScript/Jest contract run on 2026-08-28; not released
- Observed result: same scoped key/hash returns the stored operation and `deduplicated=true` without a second ledger/audit mutation; same key with changed payload or source returns `IDEMPOTENCY_KEY_REUSED` 409
- Rollback rehearsal: no database or route adoption exists; revert `36c710d` without data cleanup
- ADR/project-memory/audit/Graphify updates: implements the proposed ADR-0002 interface only; it does not promote the ADR or claim database facts, so project memory/Graphify remain unchanged
- Remaining risks: P2002 currently maps to a stable retryable conflict but true concurrent losers are not yet converted to committed replay results; exact Prisma composite names/JSON types and transaction-abort behavior must be validated after schema migration. PASS-REAL and REGRESSION_VERIFIED are explicitly pending
