# P1-C-04 Evidence

- Task ID: P1-C-04
- Evidence status: PASS-REAL for local PostgreSQL atomic confirmation and route contract
- Baseline commit: `decdbe6` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@66970be`
- Source evidence: `backend/src/services/importService.ts`, `backend/src/routes/import.ts`, `backend/src/services/prismaFinancialMutationStore.ts`, focused route tests, and `importConfirm.phase1.integration.test.ts`
- Affected families/roles/routes/tables: authenticated `admin`/`member` `POST /api/families/:familyId/import/confirm`; centralized write authorization precedes the transactional service; batch and rows are family-scoped; ledger facts use the authenticated actor and `IMPORT` source
- RED command: `npm test -- --runInBand src/routes/import.phase1.test.ts`; the new batch-only contract initially failed because the legacy route required client `items` and returned the wrong contract. The old client-owned payload path was then explicitly locked to 400 / `VALIDATION_FAILED` / zero ledger mocks
- RED failure summary: valid batch-only requests returned 400 `items 不能为空`; invalid server-row and canonical-value behavior was absent. This was a product contract failure, not an environment failure
- GREEN scope: confirm accepts only strict `batchId`, `expectedPreviewHash`, optional row-number keyed `categoryPatch`, and `Idempotency-Key`; it family-scopes the batch, verifies hash/status/expiry/row validity, claims `PREVIEWED → CONFIRMING`, invokes Income/Expense application services through a transaction-backed Ledger adapter, updates row references, and commits the batch as `COMMITTED`
- Atomicity scope: every row is validated before any ledger create; invalid rows return `failedRows` and leave the batch `PREVIEWED`; any transaction failure rolls back batch claim, row state, ledger records, row idempotency records, batch idempotency record and audit events
- REFACTOR summary: extracted transaction-backed Prisma Ledger store reuse, centralized stable mutation error response, removed direct route-level Income/Expense writes, and preserved only category as an editable import patch; no client date/amount/type is accepted
- Regression commands/results: focused import/parser/route/CORS/schema tests PASS, 5 suites / 24 tests; `npm run build` PASS; `npm run test:integration` PASS, 7 suites / 41 tests; role matrix PASS-REAL, including member batch preview/confirm
- Security negative matrix: real PostgreSQL tests cover invalid rows, hash tamper, expired batch, cross-family batch, client-owned `items`, and committed batch reuse with zero new ledger writes; unauthenticated/viewer/unknown-role/non-member denial remains covered by the role matrix
- Database/migration evidence: PostgreSQL 18.1 at `localhost:5433/homefinance_phase1_test`; valid confirm verified canonical amount/date/type/description, controlled category patch, `ImportBatch.COMMITTED`, `ImportRow.COMMITTED` references and Income/Expense rows
- Frontend/E2E evidence: no frontend code changed; browser E2E NOT_RUN; P1-C-06 must migrate the client to persist batch/hash and send category patches
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: `REGRESSION_VERIFIED / AT_RISK`; server-owned atomic confirm is verified locally, but release gates, global branch threshold and frontend migration remain open
- Rollback rehearsal: not run. Disable the confirm route or restore the last verified commit while retaining additive ImportBatch/ImportRow and audit/idempotency facts; do not reinstate implicit partial success or client-owned financial fields
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable; tracker and project memory updated. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: idempotency/replay and batch claim behavior are locally verified but need broader release/upgrade/restore observation; frontend batch state, cleanup of expired previews, full infrastructure, E2E, global coverage and semantic Graphify remain open
