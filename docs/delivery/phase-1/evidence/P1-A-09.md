# P1-A-09 普通 Liability CRUD 事务化迁移

## 当前状态（2026-09-02）

- Task ID：P1-A-09
- Lifecycle：`GREEN_MINIMAL`（仅 POST 首片段；完整 CRUD 未闭环）
- Evidence status：`PASS-MOCK`（聚焦单元/路由 mock 证据）
- Health：`AT_RISK`
- Branch：`codex/phase1-ledger-trust`
- Current commit：`cb75891`
- Baseline：`d4da394`
- Design：`docs/superpowers/specs/2026-09-02-liability-crud-transactional-design.md`
- Implementation plan：`docs/superpowers/plans/2026-09-02-liability-crud-transactional-plan.md`

## 已完成的最小切片

- `POST /api/families/:familyId/liabilities` 已通过 `createLiability` 接入 `FinancialMutationCoordinator`，使用 `MANUAL` source、`Idempotency-Key` 和统一 mutation response/error helper。
- Liability create command 已复用 Balance mutation 的金额、日期、币种和 scope 校验；写入仍限定在 transaction-scoped `liability.create` adapter。
- Liability GET 已使用集中式 `requireFamilyAccess`；原有列表与分页响应保持兼容。
- 边界测试与 Balance service 测试已覆盖上述最小路径，未声称 PUT/DELETE 或真实数据库已完成。

## 验证记录

| Command | Result |
| --- | --- |
| `backend> npm test -- --runInBand src/routes/liabilities.test.ts` | PASS — 1 suite / 9 tests |
| `backend> npm test -- --runInBand src/services/balanceMutationService.test.ts` | PASS — 1 suite / 7 tests |
| `backend> npm test -- --runInBand` | PASS — 50 suites / 419 tests |
| `backend> npm run build` | PASS |
| `git diff --check` | PASS before commit |

`e45383a` 定义了 route boundary 测试，`cb75891` 实现并提交了 POST 首片段。此前没有单独保存 RED 命令输出，因此不把未记录的失败日志伪装成证据。

## 尚未完成与阻塞

- PUT/DELETE 仍保留 route-local family lookup 和直接 `prisma.liability.update/delete`；尚未满足“所有 mutation 单一入口”的验收。
- `Liability.version` additive migration、transaction adapter 的 CAS 读写、update/delete service command 尚未实现。
- 真实 PostgreSQL HTTP CRUD、同 key replay/key conflict、并发、stale version、viewer/非成员零副作用和故障回滚尚未执行；因此不能标记 `PASS-REAL` 或 `REGRESSION_VERIFIED`。
- Balance query/valuation/currency conversion 不在本切片范围，仍是独立待办。
- Docker/Compose、Redis、MinIO、Playwright、populated restore、staging/release observation 和 semantic Graphify refresh 继续按全局 tracker 保持 `BLOCKED`/`NOT_RUN`。

## 下一步与回滚

1. 先补 Liability update/delete 的 RED 合同和版本 schema/migration，再实现 transaction adapter 与 HTTP route adoption。
2. 在 Docker/Compose 之外先运行本机 PostgreSQL integration；记录真实 migration、replay/concurrency、授权负向和 transaction rollback 证据。
3. 完成后再更新本卡、tracker、project memory 和审计风险表；未完成前不要合并到 `main`。

本次没有新增 migration；若需要回退最小切片，可单独回退 `cb75891`，不影响既有 Asset/Income/Expense 事务路径。

## 范围声明

本任务目标仍是普通 Liability CRUD 的事务、幂等、审计、cacheVersion 和 family/version 条件写入。估值、汇率换算、外部依赖、E2E、生产升级/恢复和 semantic Graphify refresh 不属于本切片。
