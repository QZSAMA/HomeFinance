# P1-G-00 Evidence

- Task ID: P1-G-00
- Evidence status: PASS-MOCK
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; RED `ad17629`; implementation `a726668`; regression-tooling amendment `db98a00`
- Source evidence: `docs/superpowers/specs/2026-08-28-homefinance-phase1-design.md` §3.1, §9, §10; current `backend/src/app.ts` listener and external initialization
- Affected families/roles/routes/tables: process boundary; all routes import app transitively; no family mutation semantics changed by this task
- RED command: `npm test -- --runInBand src/app.test.ts`
- RED failure summary: `npm test -- --runInBand src/app.test.ts` failed at `expect(listen).not.toHaveBeenCalled()` with one call `app.listen(8080, ...)`; Jest also reported an open `TCPSERVERWRAP`. Prisma, route and external boundaries were isolated only to let the real app assembly reach this target assertion.
- GREEN command/result: `npm test -- --runInBand src/app.test.ts` — PASS (1 suite, 1 test); app import opened no listener and did not call Redis/MinIO initialization.
- GREEN scope: added `src/db/prisma.ts` and `src/server.ts`; removed listener/Redis/MinIO startup from `src/app.ts`; migrated production Prisma imports and related test mocks; updated `package.json` entrypoint/scripts without changing route behavior.
- REFACTOR summary: typed `createApp(): Express`, exported the named `app`, made `startServer` shutdown idempotent, and corrected `package.json.main` to `dist/server.js`; focused test remained green.
- Regression commands/results: matching local Prisma 5.22 engines regenerated the client; `npm run build` PASS; focused app/Ledger tests PASS. Test-first Jest config repairs proved and fixed Windows path matching and made only exact `RUN_INTEGRATION=1` opt into the database suite. The default run passes 30 suites / 256 tests, but `npm test -- --runInBand --coverage` exits 1 because global branch coverage is 43.17% against the unchanged 60% threshold. `npx prisma validate` PASS; `npx prisma format --check` reports the pre-existing schema is unformatted and exits with a Windows libuv assertion.
- Security negative matrix: DESIGNED where applicable; execution evidence not yet available
- Database/migration evidence: NOT_RUN; no schema or migration changes are in scope. Prisma Client generation was attempted and blocked as recorded above.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: PASS-MOCK for app isolation and default unit regression; generated-client parity/build are restored. Real startup dependencies and infrastructure remain unobserved.
- Rollback rehearsal: revert process-boundary commit `a726668`; this task changed no schema or persisted data, so no data cleanup is required
- ADR/project-memory/audit/Graphify updates: No ADR or schema change; project memory now records the process boundary and PASS-MOCK limit. Graphify code/semantic refresh was reviewed: generated coverage nodes were excluded and no real-infrastructure claim was added.
- Remaining risks: global branch coverage and Prisma formatting gates fail; PostgreSQL credentials, Redis, MinIO and real startup remain unobserved. The focused test mocks external boundaries and is not real-infrastructure evidence, so lifecycle remains REFACTORED/AT_RISK rather than REGRESSION_VERIFIED.
