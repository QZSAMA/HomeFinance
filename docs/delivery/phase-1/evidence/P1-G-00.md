# P1-G-00 Evidence

- Task ID: P1-G-00
- Evidence status: BLOCKED
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; implementation commit (see final commit hash)
- Source evidence: `docs/superpowers/specs/2026-08-28-homefinance-phase1-design.md` §3.1, §9, §10; current `backend/src/app.ts` listener and external initialization
- Affected families/roles/routes/tables: process boundary; all routes import app transitively; no family mutation semantics changed by this task
- RED command: `npm test -- --runInBand src/app.test.ts`
- RED failure summary: `npm test -- --runInBand src/app.test.ts` failed at `expect(listen).not.toHaveBeenCalled()` with one call `app.listen(8080, ...)`; Jest also reported an open `TCPSERVERWRAP`. Prisma, route and external boundaries were isolated only to let the real app assembly reach this target assertion.
- GREEN command/result: `npm test -- --runInBand src/app.test.ts` — PASS (1 suite, 1 test); app import opened no listener and did not call Redis/MinIO initialization.
- GREEN scope: added `src/db/prisma.ts` and `src/server.ts`; removed listener/Redis/MinIO startup from `src/app.ts`; migrated production Prisma imports and related test mocks; updated `package.json` entrypoint/scripts without changing route behavior.
- REFACTOR summary: typed `createApp(): Express`, exported the named `app`, made `startServer` shutdown idempotent, and corrected `package.json.main` to `dist/server.js`; focused test remained green.
- Regression commands/results: `npm run build` — BLOCKED by stale generated Prisma Client types (`Family.cacheVersion` missing in `familyAccess.ts` and the existing database integration test); `npm run prisma:generate` — BLOCKED by external download `ECONNRESET` from `binaries.prisma.sh`; full coverage/regression not run after this environment blocker.
- Security negative matrix: DESIGNED where applicable; execution evidence not yet available
- Database/migration evidence: NOT_RUN; no schema or migration changes are in scope. Prisma Client generation was attempted and blocked as recorded above.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: PASS-MOCK for app isolation focused test; real startup dependencies and generated-client parity remain unobserved.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: No ADR or schema change; project memory remains valid for the process-boundary decision. Graphify refresh is deferred with the generated-output scope review. RED and focused GREEN evidence are recorded here.
- Remaining risks: Prisma client generation/toolchain is blocked by `ECONNRESET`; build and full regression remain unverified. The focused test mocks Redis/MinIO/Prisma boundaries and is not real-infrastructure evidence.
