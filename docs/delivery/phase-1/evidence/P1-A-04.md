# P1-A-04 Evidence

- Task ID: P1-A-04
- Evidence status: PASS-MOCK for the route-level negative matrix; PASS-REAL for authenticated-member Income CRUD HTTP-to-PostgreSQL behavior and shared Ledger/Prisma persistence
- Baseline commit: `4bd2609` on `codex/phase1-ledger-trust`
- Source evidence: `backend/src/routes/incomes.ts`, `backend/src/routes/incomes.phase1.test.ts`, `backend/src/tests/ledgerRoutes.phase1.integration.test.ts`, `backend/src/services/ledgerApplicationService.ts`, `backend/src/services/prismaFinancialMutationStore.ts`
- Affected families/roles/routes/tables: `POST|PUT|DELETE /api/families/:familyId/incomes`; `Income`, `IdempotencyRecord`, `AuditEvent`, and existing `Family.cacheVersion` trigger behavior
- RED command: `npm test -- --runInBand src/routes/incomes.phase1.test.ts src/routes/expenses.phase1.test.ts`
- RED failure summary: migration contracts initially failed because Income mutations bypassed Ledger. Later focused REDs showed `Idempotency-Replayed` was absent and schema validation responses lacked `VALIDATION_FAILED`/`retryable`.
- GREEN scope: all Income mutations now call `createIncome`, `updateIncome`, or `deleteIncome`; the route has no direct Income create/update/delete. `If-Match` accepts integer/quoted/weak ETag forms, while a missing header remains backward-compatible and makes the service reread the stored version inside its transaction. Create retains 201, update/delete keep compatible bodies and add `version`, `operationId`, and `deduplicated`; replay returns `Idempotency-Replayed: true`.
- REFACTOR summary: shared idempotency-key parsing, version parsing, response shaping, replay header and stable error mapping moved to `ledgerRouteSupport.ts`. CORS exposes the new replay header to browser clients.
- Regression commands/results: focused route/security/cache PASS, 4 suites / 18 tests; focused service/permissions PASS, 5 suites / 45 tests; `npm run build`, `npm run test:integration` (5 suites / 32 tests), `npx prisma validate`, and `npx prisma format --check` PASS. The full coverage run executes 37 suites / 322 tests with all assertions passing, but correctly exits non-zero only on the unchanged global 60% branch gate (68.54% statements, 50.48% branches, 66.76% functions, 69.08% lines).
- Security negative matrix: PASS-MOCK for Income-specific viewer/non-member rejection before Ledger/direct write, malformed input before Ledger, and stable stale-version error. PASS-REAL through `roleMethodMatrix.integration.test.ts` for all registered family-scoped mutation entries: unauthenticated 401 and viewer/unknown-role/non-member 403 with zero family side effects.
- Database/migration evidence: no migration. Local PostgreSQL 18.1 `homefinance_phase1_test` integration suite PASS, 5 suites / 32 tests. The real HTTP route contract creates an Income, replays the same idempotency key without a second Income/IdempotencyRecord/AuditEvent, updates using `If-Match: W/"1"`, rejects stale version `1` with `VERSION_CONFLICT`, and deletes version `2` while proving no Income remains for the family.
- Frontend/E2E evidence: no frontend flow changed. Lint exits 0 with 16 pre-existing warnings; build passes with the known oversized main chunk. Browser E2E NOT_RUN.
- Release environment/time: local Windows, 2026-08-31; no staging/release observation.
- Observed result: REFACTORED / AT_RISK. The user-approved route-adoption exception proceeded while P1-A-03 remains AT_RISK on the global coverage gate; this evidence does not change that dependency risk.
- Rollback rehearsal: not run. Revert the route adapter as one entry point only; do not dual write and do not delete persisted idempotency/audit facts.
- ADR/project-memory/audit/Graphify updates: ADR-0001 and ADR-0002 remain the governing decisions. Tracker and project memory are updated. Semantic Graphify update is pending because the available AST-only command would replace the reviewed semantic graph.
- Remaining risks: global branch coverage, complete positive real role/method verification, P1-B-04 unclassified historical error, upgrade/restore, Redis/MinIO/Compose, E2E, staging/release, and semantic graph tooling.
