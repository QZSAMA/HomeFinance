# P1-C-03 Evidence

- Task ID: P1-C-03
- Evidence status: PASS-REAL for the local PostgreSQL route and persistence contract
- Baseline commit: `b9eff6d` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@decdbe6`
- Source evidence: `backend/src/services/importService.ts`, `backend/src/routes/import.ts`, `backend/src/app.ts`, focused tests, and `roleMethodMatrix.integration.test.ts`
- Affected families/roles/routes/tables: authenticated `admin`/`member` `POST /api/families/:familyId/import/csv`; `requireFamilyWriteAccess` authorizes before parsing/persistence; server writes family-scoped `ImportBatch` and nested `ImportRow`
- RED command: `npm test -- --runInBand src/routes/import.phase1.test.ts`; observed 1 failed / 15 passed because the route returned no server-owned batch or preview headers and did not call `persistImportPreview`
- RED failure summary: expected `x-import-batch-id=batch_server_owned`, received `undefined`; failure was the missing P1-C-03 behavior, not an environment error
- GREEN scope: `persistImportPreview` computes SHA-256 `fileHash`, deterministic normalized `previewHash`, parser version `csv-v1`, and persists `PREVIEWED` batch rows with canonical server-parsed payloads; `/csv` retains the legacy array body and adds `X-Import-Batch-Id` and `X-Import-Preview-Hash`; CORS exposes both headers
- REFACTOR summary: removed the duplicate route-local membership query and consumed the membership already established by centralized write authorization; a focused test locks the single authorization lookup boundary
- Regression commands/results: focused import/parser/route/CORS/schema tests PASS, 5 suites / 24 tests; `npx prisma validate`, `npx prisma format --check`, and `npm run build` PASS; `npm run test:integration` PASS, 6 suites / 36 tests; role matrix PASS-REAL, 4 suites / 4 tests
- Security negative matrix: existing real role matrix continues to prove 401 for unauthenticated and 403 for non-member/viewer/unknown-role before mutation; positive member preview now verifies actor and family IDs in persisted batch; no duplicate route-local authorization lookup remains
- Database/migration evidence: PostgreSQL 18.1 at `localhost:5433/homefinance_phase1_test`; route integration verified the returned batch/header hash, `ImportBatch.familyId`, `actorUserId`, `format`, `parserVersion`, `PREVIEWED` state, row count, and canonical `ImportRow` payload
- Frontend/E2E evidence: no frontend code changed; CORS contract is PASS-MOCK; browser E2E NOT_RUN
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: `REFACTORED / AT_RISK`; server-owned preview persistence is verified, but P1-C-04 has not yet changed confirm to accept only batch/hash or made confirmation atomic/idempotent
- Rollback rehearsal: not run. Keep the additive tables and use forward repair/restore; the compatibility array response can be retained while the new confirm contract is introduced
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable; tracker and project memory updated with the preview adoption evidence. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: legacy client-supplied `items` confirm remains a financial mutation bypass; no batch tamper/cross-family/expiry rejection, atomic confirm, Ledger delegation, replay/concurrency, frontend batch state flow, upgrade/restore, global branch threshold, E2E, or release observation yet
