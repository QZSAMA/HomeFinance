# P1-E-03 Evidence

- Task ID: P1-E-03
- Evidence status: REGRESSION_VERIFIED
- Baseline commit: 081084a
- Branch/commit: `codex/phase1-ledger-trust@7f6c366`
- Source evidence: `docs/superpowers/specs/2026-08-28-homefinance-phase1-design.md`, Task 9, and ADR-0004
- Affected families/roles/routes/tables: family-scoped `/ai/chat` and `/ai/ocr`; member/admin proposal creation; viewer/non-member denial; `AiProposal`/`AiProposalItem` plus source `AiConversation`; Income/Expense/Asset/Liability remain unchanged before confirmation
- RED command: `cd backend; npm test -- --runInBand src/services/aiProposalService.test.ts`, `npm test -- --runInBand src/routes/ai.test.ts -t "malformed action"`, and the opt-in PostgreSQL route suite `RUN_INTEGRATION=1 npm test -- --runInBand src/tests/aiProposalRoutes.phase1.integration.test.ts`
- RED failure summary: Focused REDs observed the service accepting viewer/empty/query actions, retaining unknown fields in confirmable item data, trusting the caller role, and the route returning 500 without a stable error contract for malformed AI output. The integration suite requires the repository's explicit `RUN_INTEGRATION=1` switch; its PostgreSQL assertions pass while unavailable Redis emits the known fail-open rate-limit warning.
- GREEN commit and scope: `7f6c366` adds `aiProposalService` and route persistence for text, OCR, image-only and image-plus-text paths. The service checks current family membership before proposal creation, ignores the caller role for the actor snapshot, validates source conversation/file ownership and source type, rejects query/delete/unknown actions, normalizes allowed create actions, computes a 15-minute server expiry and canonical SHA-256 hash, and writes ordered proposal items. Routes return `proposalId`, version, hash and expiry, never call `executeActions` on these proposal-producing paths, and return stable `DomainError`/`INTERNAL_ERROR` response fields.
- REFACTOR summary: `financialMutationCoordinator` now exports the shared canonicalizer and orders object keys by deterministic code-unit comparison rather than locale-sensitive `localeCompare`; proposal item data is derived from normalized allow-listed fields while the canonical original payload remains the recorded AI evidence. No coverage threshold or legacy `/execute-actions` assertion was weakened.
- Regression commands/results: `npm run build` PASS; `npx prisma validate` and `npx prisma format --check` PASS against the non-production PostgreSQL URL; focused AI/service/permission regression PASS, 82 tests; full default backend `npm test -- --runInBand` PASS, 44 suites / 361 tests; real PostgreSQL `npm run test:integration` with `RUN_INTEGRATION=1` PASS, 10 suites / 54 tests. Required `npm test -- --runInBand --coverage` executes 44 suites / 361 tests with all assertions passing but exits non-zero because branches are 49.10% versus the unchanged 60% threshold (statements 67.55%, functions 64.77%, lines 68.14%).
- Security negative matrix: Real PostgreSQL HTTP evidence covers member text and OCR proposal persistence with zero changes to all four financial tables before confirmation, and viewer/non-member requests are rejected before provider calls, proposal writes or financial reads/writes. Focused service tests cover forged role, viewer, empty/query actions, malformed amounts/dates, unknown fields, unknown source, action-count limit, invalid clock, and text/file source mismatch. The legacy raw `/execute-actions` path remains an open P1-E-04 risk and is not claimed safe by this card.
- Database/migration evidence: PASS-REAL on local PostgreSQL 18.1 at `localhost:5433`. The new `aiProposalRoutes.phase1.integration.test.ts` passes 4/4; it verifies source conversation ownership/type, actor snapshot, proposal status/version/hash/expiry, ordered item persistence and zero Income/Expense/Asset/Liability count changes. Existing AI proposal schema and all other integration suites remain green. Redis and MinIO were not available; their warnings are recorded and do not affect the PostgreSQL assertions.
- Frontend/E2E evidence: No frontend code changed in this slice; frontend component tests and browser E2E are NOT_RUN for server-owned proposal ID/state migration. P1-E-05 remains responsible for replacing the legacy raw-action confirmation request.
- Release environment/time: local Windows, PostgreSQL 18.1, 2026-09-01; staging/release observation NOT_RUN
- Observed result: `REGRESSION_VERIFIED / AT_RISK`. Chat/OCR now persist server-owned proposal facts and remain proposal-only through the new paths. Atomic confirmation, Balance/Ledger confirmation orchestration, replay/concurrency, expiry cleanup, legacy adapter removal, external infrastructure and release observation remain open.
- Rollback rehearsal: No new migration was introduced by `7f6c366`; the prior additive proposal migration remains forward-only. A rollback can disable/revert proposal-producing route code while preserving `AIProposal` facts; do not drop populated proposal tables. Full staging rollback/restart rehearsal remains open.
- ADR/project-memory/audit/Graphify updates: This card, tracker, project memory and P1-G-06 are updated with the new evidence. ADR-0004 remains Accepted. Semantic Graphify refresh is still pending because the installed updater is AST-only and must not replace the reviewed semantic graph.
- Remaining risks: P1-E-04 must implement proposalId/version/hash confirmation with atomic Ledger/Balance mutation, audit/result persistence, replay/concurrency and zero-side-effect tamper/expiry handling. P1-E-05/P1-E-06 must migrate and test the frontend and the complete negative matrix; `execute-actions` remains a compatibility bypass until then.
