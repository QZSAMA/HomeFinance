# P1-G-05 Evidence

- Task ID: P1-G-05
- Evidence status: PASS-REAL
- Baseline commit: `fa43c16`
- Verified branch/commit: `codex/p1-g05-infra-recovery@5332588d1642d5da84771318c0b1b36961d69fe7`
- Green run: [GitHub Actions 34091443392](https://github.com/QZSAMA/HomeFinance/actions/runs/34091443392)
- Pull request: [#5 P1-G-05 infrastructure recovery evidence](https://github.com/QZSAMA/HomeFinance/pull/5) (draft at evidence capture)
- Scope: Redis stale-key outage/recovery, MinIO single-file lifecycle/outage/recovery, and unauthenticated/non-member/viewer/cross-family object isolation on the disposable six-service Compose stack. No production or staging data was used.

## RED and failure-to-green record

Focused Jest tests were committed before production changes as `c078008`:

```text
backend> npm test -- src/routes/files.test.ts --runInBand
Test Suites: 1 failed, 1 total
Tests:       6 failed, 12 passed, 18 total
```

The failures matched the approved design: complete MinIO upload failure returned 201, database-create failure did not compensate the object, delete still used an unscoped `findUnique`, and MinIO delete failure could not preserve the row with a retryable response. `f560b40` made the minimum route change and the focused suite became 18/18.

The first PR CI attempt then exposed a test-boundary defect in `roleMethodMatrix.integration.test.ts`: its PostgreSQL-only positive file-delete fixture created a row without a MinIO object, so the new correct outage behavior returned 503 instead of the old swallowed-error 200. Commit `5332588` isolates that database/role matrix from MinIO and still asserts the object-delete adapter call. The next real PostgreSQL run passed 17 suites / 97 tests. The real MinIO lifecycle is not mocked in the P1-G-05 recovery workflow.

Run `34090874241` proved the first complete recovery pass but emitted the new GitHub action-runtime deprecation annotation from `actions/*@v4`. Commit `e2918ea` moved this new workflow to `checkout@v5`/`setup-node@v5`; final run `34091443392` repeated the full gate without workflow warnings. No security, stale-value, side-effect, status, or cleanup assertion was weakened, and no arbitrary sleep was introduced.

## Real recovery evidence

- Runner: GitHub-hosted `ubuntu-latest`, resolved to Ubuntu 24.04.4 / image `ubuntu-24.04@20260831.293.1`; Node `20.20.2`; repository token permission `contents: read`.
- Job: `Redis and MinIO recovery against Compose`, successful in 1m55s.
- Startup: PostgreSQL, Redis, MinIO, mock-AI, backend, and frontend all became healthy before fault injection.
- Named checkpoints from the same run:

| Checkpoint | Time | Verified behavior |
| --- | ---: | --- |
| `identity-and-family-setup` | 419ms | Three users, two isolated families, and a real family-A viewer membership created through public APIs |
| `redis-miss-hit-and-persisted-stale-key` | 214ms | Income 100, report MISS→HIT, exact versioned Redis key discovered and persisted |
| `redis-outage-authorized-fresh-read-write` | 285ms | Redis stopped; unauthenticated 401, family-B admin 403, authorized report 200; second income committed and fresh total became 125 |
| `redis-recovery-miss-hit-without-backend-restart` | 2732ms | Redis restarted with old key still present; backend reconnected without restart; new version returned 125 MISS→HIT and never stale 100 |
| `minio-object-isolation-matrix` | 3465ms | Real upload/object stat/signed URL; 401/403/viewer/cross-family 404 matrix; denied mutations preserved the exact family-A row and object |
| `minio-outage-preserves-file-row` | 10179ms | MinIO stopped; upload and delete returned 503/`STORAGE_UNAVAILABLE`; failed upload created no row and failed delete retained metadata |
| `minio-recovery-and-second-lifecycle` | 1864ms | Original row/object survived restart, retry deleted both, and a second upload/stat/delete lifecycle passed without backend restart |

Redis correctness came from the PostgreSQL `Family.cacheVersion`, not old-key deletion. MinIO object checks used the exact API-returned path and direct `statObject`/prefix listing from the backend network. Family-B listings contained no family-A ID, path, metadata, or signed URL.

## Cleanup evidence

The `if: always()` teardown ran `docker compose ... down --volumes --remove-orphans` successfully. Logs explicitly show removal of frontend, backend, MinIO, PostgreSQL, Redis, and mock-AI containers; `homefinance-e2e_minio_e2e_data`; `homefinance-e2e_postgres_e2e_data`; and `homefinance-e2e_default` network. A passing runner with failed or skipped teardown would not have qualified as PASS-REAL.

## Regression and contract evidence

| Command/run | Result |
| --- | --- |
| `backend> npm test -- src/routes/files.test.ts --runInBand` | PASS — 18/18 |
| Files + family permission focused run | PASS — 2 suites / 32 tests |
| `backend> npm run build` | PASS |
| `backend> npm test -- --runInBand --coverage` | PASS — 55 suites / 467 tests; statements 77.89%, branches 61.95%, functions 73.28%, lines 79.31% |
| Actions CI `34091443383` | PASS — Backend Tests, Frontend Build, and PostgreSQL integration 17 suites / 97 tests |
| `backend> npx prisma validate` | PASS — non-production URL |
| `backend> npx prisma format --check` | EXISTING GATE FAILURE — schema remains unformatted and this branch has no schema diff |
| `frontend> npm run lint` | PASS — no lint output |
| `frontend> npm run build` | PASS — only the existing 870.27 kB / 241.20 kB gzip chunk warning |
| `frontend> npx playwright test --list` | PASS — four P1-G-04 journeys discovered |
| Browser E2E `34091443379` | PASS — same SHA, four journeys and final teardown |
| Local `docker --version` | ENVIRONMENT BLOCKED — no Docker CLI; the authoritative Actions run is green |

## Result, rollback, and remaining risks

- `backend/src/routes/files.ts` now uses centralized family policy before file DB/storage work, returns retryable 503 on MinIO upload/delete failure, preserves the row on failed delete, uses family-scoped object lookup, and attempts compensation after upload/DB-create failure. ADR-0008 records this cross-system lifecycle decision.
- P1-G-05 is `PASS-REAL` for this disposable single-file recovery topology. It is not `RELEASED`, `OBSERVED`, production availability evidence, a populated restore rehearsal, or multi-file distributed atomicity proof.
- Durable outbox/orphan reconciliation, multi-file atomic batches, production MinIO topology, populated restore, staging/release/rollback observation, P1-H-03, and P1-H-04 remain open.
- Semantic Graphify refresh remains pending because the installed runner is AST-only and would overwrite the reviewed semantic graph; no misleading refresh was performed.
