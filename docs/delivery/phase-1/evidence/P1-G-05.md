# P1-G-05 Evidence

- Task ID: P1-G-05
- Evidence status: BLOCKED (environment unavailable)
- Baseline commit: 081084a
- Branch/commit: `codex/phase1-ledger-trust@9e89525`
- Source evidence: `docker-compose.e2e.yml`, `backend/Dockerfile`, `frontend/Dockerfile`, `frontend/nginx.conf`, and `frontend/scripts/run-e2e.mjs`
- Affected families/roles/routes/tables: PostgreSQL, Redis and MinIO lifecycle plus backend/frontend container health and `/api` proxy; no production data volume
- RED command: `cd frontend; npm run test:e2e`
- RED failure summary: Docker availability check fails before Compose startup; Docker Desktop/CLI is unavailable on the local Windows host.
- GREEN scope: The E2E Compose definition includes isolated PostgreSQL, Redis, MinIO and mock AI services, health checks, and backend/frontend dependencies. It has not been executed.
- REFACTOR summary: Backend production image now runs `prisma migrate deploy` before `dist/server.js`; Prisma CLI is available in production dependencies so migration deploy can run in the container.
- Regression commands/results: `npm run test:e2e` BLOCKED at Docker preflight; no infrastructure PASS-REAL claim.
- Security negative matrix: DESIGNED where applicable; execution evidence not yet available
- Database/migration evidence: BLOCKED — the Compose PostgreSQL/Redis/MinIO services and migration startup path were not executed because Docker is unavailable.
- Frontend/E2E evidence: BLOCKED; Playwright test list is available, but Compose service health, Redis, MinIO and proxy behavior are not observed.
- Release environment/time: local Windows, 2026-09-01; Docker Desktop unavailable; staging/release observation NOT_RUN
- Observed result: BLOCKED pending Docker Desktop/CLI availability; do not treat the harness definition as real dependency evidence.
- Rollback rehearsal: Not applicable to production data; the disposable E2E stack must be removed with the documented Compose `down --volumes --remove-orphans` command after a Docker-capable run. Production rollback remains forward-fix only.
- ADR/project-memory/audit/Graphify updates: Tracker and project memory record the environment blocker; semantic Graphify refresh remains pending.
- Remaining risks: Docker availability, real Redis/MinIO failure and recovery, object isolation, populated restore and release observation remain open.

## GitHub Actions handoff (2026-09-03)

- `.github/workflows/e2e.yml` now provides a Docker-capable GitHub-hosted
  runner for the isolated Compose stack, removing the dependency on the local
  Windows Docker installation for the next verification attempt.
- This does not close P1-G-05: Redis restart/degradation, MinIO lifecycle and
  cross-family object isolation still require an observed runner or staging
  execution and must remain `NOT_RUN`/`BLOCKED` until then.
- GitHub Actions run `33740088163` confirmed that the disposable stack can pull
  and build its images and bring PostgreSQL, Redis, MinIO, mock-AI, and backend
  to healthy status. Frontend health-check failure prevented the remaining
  Compose and browser assertions; the next run includes service diagnostics.
