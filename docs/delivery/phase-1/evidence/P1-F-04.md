# P1-F-04 Evidence

- Task ID: P1-F-04
- Evidence status: REGRESSION_VERIFIED
- Baseline commit: 081084a
- Branch/commit: codex/p1-f-timezone; pending implementation commit
- Source evidence: `docs/superpowers/specs/2026-09-03-family-timezone-period-currency-design.md` §9; implementation plan Task 7
- Affected families/roles/routes/tables: writable family members; `POST /api/families/:familyId/goals/:goalId/contributions`, `GET .../goals/progress`, `Goal.currency`, `GoalContribution`
- RED command: `cd backend; npm test -- --runInBand src/services/goalContributionService.test.ts`
- RED failure summary: Before implementation, the service module did not exist; the old goals progress path read shared family assets/liabilities and could not represent source allocation or isolation.
- GREEN commit and scope: Pending commit; adds additive Goal currency/contribution schema and migration, atomic contribution service with source-family validation, scoped idempotency/audit, and contribution-based progress.
- REFACTOR summary: Explicit source keys prevent double attribution, replay returns the stored result, no-contribution and mixed-currency progress are nullable/unavailable, and viewer authorization is enforced before service access.
- Regression commands/results: `npm test -- --runInBand src/routes/goals.test.ts src/services/goalContributionService.test.ts` → 16/16; `npm run build` → PASS; `npm test -- --runInBand` → 55 suites / 458 tests PASS.
- Security negative matrix: PostgreSQL integration verifies viewer POST returns 403, cross-family source returns 404, and contribution count remains unchanged; same-source second goal returns 409 with zero additional row.
- Database/migration evidence: additive migration `20260903100200_add_goal_contributions` applied on PostgreSQL 18.1 at `localhost:5433`; schema includes amount/source/currency checks, family-scoped allocation/source unique keys, cascade relations and cache-version trigger.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: Local PostgreSQL goal contribution integration 2/2 PASS; Docker/Redis/MinIO/staging remain unobserved.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: To be recorded with the implementation commit; semantic Graphify refresh remains pending because only AST-only tooling is available.
- Remaining risks: Frontend goal contribution UI, Balance valuation/as-of, release and external infrastructure gates remain open.
