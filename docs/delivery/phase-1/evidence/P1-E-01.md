# P1-E-01 Evidence

- Task ID: P1-E-01
- Evidence status: PASS-MOCK for the original text-chat proposal-only slice; later Phase 1 cards record server-owned proposal persistence and supported atomic confirmation
- Baseline commit: 3cf56bc on codex/phase1-ledger-trust
- Branch/commit: codex/phase1-ledger-trust@5a564c9
- Source evidence: backend/src/routes/ai.ts, backend/src/routes/ai.test.ts, frontend/src/pages/AIPage.tsx, frontend/src/pages/AIPage.test.tsx, and ADR-0004
- Affected families/roles/routes/tables: authenticated family writer POST /api/families/:familyId/ai/chat; the text branch still reads family finance context and stores AIConversation, but no longer invokes executeActions before user confirmation
- RED command: npm test -- --runInBand src/routes/ai.test.ts -t "returns text actions as proposals without executing them"
- RED failure summary: the new contract failed because proposedActions was undefined; the legacy handler called executeActions immediately and returned only its action results. This was the intended behavior RED, not an environment or compilation failure
- GREEN scope: text chat now returns actions as proposedActions, returns an empty executed actions array, computes duplicateFlags through the existing family-scoped duplicate check, and never calls executeActions. The existing frontend already renders proposedActions as an editable confirmation card and calls /execute-actions only after an explicit user click
- REFACTOR summary: reused the OCR proposal response shape instead of adding a second frontend contract; OCR/image branches and the explicit-confirmation endpoint were not changed in this slice
- Regression commands/results: focused RED then GREEN PASS, 1 test; full AI route suite PASS, 1 suite / 28 tests; AI route plus centralized family access PASS, 2 suites / 32 tests; backend npm run build PASS. Full backend coverage executes 42 suites / 344 tests with every assertion passing; statements 67.47%, branches 47.63%, functions 64.16%, lines 68.01%, and the command remains non-zero only because branches are below the unchanged 60% threshold
- Security negative matrix: the focused test proves zero executeActions calls before confirmation; existing route tests cover unauthenticated and non-member denial, centralized requireFamilyWriteAccess covers viewer/unknown roles, and OCR tests already prove no auto-execution. This remains PASS-MOCK; a real PostgreSQL zero-ledger assertion and server-owned proposal authorization/tamper/replay/concurrency matrix belong to P1-E-02~06
- Database/migration evidence: no schema or migration changed; PostgreSQL integration NOT_RUN because this safety slice stops before the legacy executor boundary rather than adding persistence
- Frontend/E2E evidence: existing AIPage proposed-action confirmation tests PASS, 1 file / 2 tests; they exercise text sendChat responses with proposedActions, failure retry, pending-click suppression, and success status. Browser/network E2E NOT_RUN
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: REFACTORED / AT_RISK. Text AI no longer automatically mutates finance data, but the confirmation endpoint is still a legacy raw-actions adapter without AIProposal ownership or atomic exactly-once semantics
- Rollback rehearsal: NOT_RUN. Reverting this commit would restore an unsafe automatic mutation path and is not an acceptable operational fallback; disable text action generation/chat mutation capability instead if a forward fix is required
- ADR/project-memory/audit/Graphify updates: ADR-0004 remains authoritative; tracker, project memory, HF-AI-001, and P1-G-03 are updated. Semantic Graphify refresh remains pending because the installed updater is AST-only and would overwrite the reviewed graph
- Remaining risks at this checkpoint: this card does not cover the later AIProposal/AIProposalItem schema, supported Income/Expense confirmation, replay/concurrency or refresh recovery. Current residual risks are Balance actions, cancellation, legacy adapter closure, external infrastructure, browser E2E and release observation; see P1-E-04~06
