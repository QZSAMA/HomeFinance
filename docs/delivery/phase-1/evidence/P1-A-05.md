# P1-A-05 Evidence

- Task ID: P1-A-05
- Evidence status: PASS-MOCK for the route-level negative matrix; PASS-REAL for authenticated-member Expense CRUD HTTP-to-PostgreSQL behavior and shared Ledger/Prisma persistence
- Baseline commit: `4bd2609` on `codex/phase1-ledger-trust`
- Source evidence: `backend/src/routes/expenses.ts`, `backend/src/routes/expenses.phase1.test.ts`, `backend/src/tests/ledgerRoutes.phase1.integration.test.ts`, `backend/src/services/ledgerApplicationService.ts`, `backend/src/services/prismaFinancialMutationStore.ts`
- Affected families/roles/routes/tables: `POST|PUT|DELETE /api/families/:familyId/expenses`; `Expense`, `IdempotencyRecord`, `AuditEvent`, and existing `Family.cacheVersion` trigger behavior
- RED command: `npm test -- --runInBand src/routes/incomes.phase1.test.ts src/routes/expenses.phase1.test.ts`
- RED failure summary: migration contracts initially failed because Expense mutations bypassed Ledger. Later focused REDs showed missing replay signalling and incomplete malformed-input error fields.
- GREEN scope: all Expense mutations now call `createExpense`, `updateExpense`, or `deleteExpense`; the route has no direct Expense create/update/delete. Versioned update/delete use the shared `If-Match` parser and service-side conditional predicate. Responses retain their former shapes and add `version`, `operationId`, and `deduplicated`; completed replays include `Idempotency-Replayed: true`.
- REFACTOR summary: shared route mechanics are concentrated in `ledgerRouteSupport.ts`; no route-local family policy was reintroduced. The cache-invalidation regression fixture now exercises the Ledger entry point while preserving the durable-version cache contract.
- Regression commands/results: focused route/security/cache PASS, 4 suites / 18 tests; focused service/permissions PASS, 5 suites / 45 tests; `npm run build`, `npm run test:integration` (5 suites / 33 tests), `npx prisma validate`, and `npx prisma format --check` PASS. The full coverage run executes 37 suites / 322 tests with all assertions passing, but correctly exits non-zero only on the unchanged global 60% branch gate (68.54% statements, 50.48% branches, 66.76% functions, 69.08% lines).
- Security negative matrix: PASS-MOCK for Expense-specific viewer/non-member and malformed-payload zero side effects plus stale-version propagation; PASS-REAL through `roleMethodMatrix.integration.test.ts` for all registered family-scoped mutation entries: unauthenticated 401 and viewer/unknown-role/non-member 403 with zero family side effects.
- Database/migration evidence: no migration. PostgreSQL 18.1 `homefinance_phase1_test` suite PASS, 5 suites / 33 tests. The real HTTP route contract creates an Expense, updates using `If-Match: 1`, rejects a stale retry with `VERSION_CONFLICT` without a second write, deletes version `2`, and proves no Expense remains for the family.
- Frontend/E2E evidence: no frontend flow changed. Lint exits 0 with 16 pre-existing warnings; build passes with the known 856.71 kB main chunk. Browser E2E NOT_RUN.
- Release environment/time: local Windows, 2026-08-31; no staging/release observation.
- Observed result: REFACTORED / AT_RISK under the user-approved P1-A-03 dependency exception. Coverage and the incomplete real role matrix prevent `REGRESSION_VERIFIED`.
- Rollback rehearsal: not run. Revert the adapter as a single entry point and never dual write; retained idempotency/audit facts require forward remediation rather than destructive rollback.
- ADR/project-memory/audit/Graphify updates: ADR-0001/0002 remain in force; tracker and project memory update with this change. Semantic Graphify is pending because AST-only refresh would degrade the reviewed graph.
- Remaining risks: global branch coverage, AI/OCR and File upload external role paths, object isolation, P1-B-04 historic unclassified error, upgrade/restore, Redis/MinIO/Compose, E2E, staging/release, and semantic Graphify tooling.
