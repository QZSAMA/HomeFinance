# P1-C-02 Evidence

- Task ID: P1-C-02
- Evidence status: PASS-REAL for the local PostgreSQL schema and migration contract
- Baseline commit: `182c9aa` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@d101816`
- Source evidence: `backend/prisma/schema.prisma`, `backend/prisma/migrations/20260828100300_phase1_add_import_batch/migration.sql`, `backend/src/tests/importSchemaContract.phase1.test.ts`, `backend/src/tests/database.import.phase1.integration.test.ts`
- Affected families/roles/routes/tables: additive `ImportBatch`/`ImportRow` persistence for family-scoped Import preview; actor is the authenticated `User`; no route behavior is adopted in this slice
- RED command: `npm test -- --runInBand src/tests/importSchemaContract.phase1.test.ts`; database RED: `npx prisma generate` followed by `RUN_INTEGRATION=1 npx jest src/tests/database.import.phase1.integration.test.ts --runInBand --forceExit`
- RED failure summary: schema contract failed because `model ImportBatch` and its migration were absent. After client generation but before deployment, every real database test failed with `The table public.ImportBatch does not exist` (`P2021`).
- GREEN commit and scope: additive `ImportBatch` stores family/actor, source format, file hash, parser version, preview hash, lifecycle status, row count, expiry and timestamps. `ImportRow` stores batch/row identity, canonical JSON, validation errors, row status, result entity type/id and timestamps. PostgreSQL adds hash/status/row-number checks, `(batchId,rowNumber)` uniqueness, useful family/status/hash indexes, and cascading family→batch→row foreign keys.
- REFACTOR summary: Prisma relations are explicit on `User` and `Family`; schema naming aligns the actor and family authorization boundaries; no legacy table, column, or raw import confirm path was removed.
- Regression commands/results: focused schema contract PASS, 1 suite / 1 test; real import schema integration PASS, 1 suite / 3 tests; `npx prisma migrate deploy`, `npx prisma validate`, `npx prisma format --check`, `npx prisma generate`, and `npm run build` PASS; full PostgreSQL integration PASS, 6 suites / 36 tests. Full default behavior tests pass all assertions, 40 suites / 329 tests, with statements 69.68%, branches 51.69%, functions 69.68%, lines 70.24%; the command remains non-zero only because the unchanged 60% global branch threshold is not met.
- Security negative matrix: no HTTP mutation route changed. Existing real matrix still proves unauthenticated 401 and viewer/unknown-role/non-member 403 before family mutation handlers; new foreign keys and family-scoped indexes prevent orphaned or cross-family batch rows at the persistence boundary.
- Database/migration evidence: local PostgreSQL 18.1 at `localhost:5433/homefinance_phase1_test` applied migration `20260828100300_phase1_add_import_batch` incrementally. The integration fixture persisted valid/invalid row states, rejected duplicate row numbers, rejected invalid hash and row number via named PostgreSQL CHECK constraints, and verified family deletion cascades ImportRow cleanup.
- Frontend/E2E evidence: no frontend code changed; browser E2E NOT_RUN.
- Release environment/time: local Windows, 2026-09-01; no staging/release observation.
- Observed result: P1-C-02 is REFACTORED / AT_RISK. The additive persistence contract and local migration behavior are verified, while server-owned preview adoption, batch confirmation, replay/concurrency and populated upgrade/restore remain open.
- Rollback rehearsal: not run. Use forward repair or restore for the additive migration; do not delete ImportBatch/ImportRow facts through a destructive down migration.
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable. Tracker and project memory record the schema and migration evidence. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph.
- Remaining risks: P1-C-03~06 preview persistence/adoption, server-owned hash/token, atomic confirm and Ledger delegation, batch replay/concurrency, client tamper resistance, migration upgrade/restore, global branch coverage, E2E, staging/release, and semantic Graphify tooling.
