# P1-B-03 Evidence

- Task ID: P1-B-03
- Evidence status: PASS-REAL
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; arbitration implementation `63ee373`; current verification slice adds no production-code change
- Source evidence: Phase 1 design §4.2, implementation plan Task 6, ADR-0002, `P1-B-04.md`, and `phase1-concurrency.integration.test.ts`
- Affected families/roles/routes/tables: family-scoped LedgerApplicationService/FinancialMutationCoordinator, Family.cacheVersion trigger, Income, IdempotencyRecord and AuditEvent; no HTTP route behavior changed
- RED command: The Task 6 real-PostgreSQL RED is recorded by P1-B-04. This evidence completion strengthens the existing same-key/different-hash acceptance assertion after the production arbitration path was already implemented.
- RED failure summary: No isolated P1-B-03 product RED was run in this verification-only slice. The original Task 6 PostgreSQL work first exposed unrecognized composite-field `P2002` metadata before `63ee373` added scoped winner arbitration; that failure and its focused RED/GREEN are recorded under P1-B-04.
- GREEN commit and scope: `63ee373` maps scoped `P2002` conflicts to the stable, non-retryable `IDEMPOTENCY_KEY_REUSED` domain error. The real database contract now proves that a conflicting reuse returns HTTP-equivalent 409 semantics and leaves Income, IdempotencyRecord, AuditEvent and Family.cacheVersion unchanged from the completed first request.
- REFACTOR summary: Replaced the suite's Income-only assertion with a fact-count snapshot plus durable family-revision snapshot. The contract now directly protects every persisted fact in the coordinator transaction and avoids reliance on suite ordering.
- Regression commands/results: `$env:RUN_INTEGRATION='1'; $env:DATABASE_URL='postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public'; npx jest src/tests/phase1-concurrency.integration.test.ts --runInBand --detectOpenHandles` PASS, 1 suite / 7 tests. `npm run test:integration` PASS, 3 suites / 27 tests. P1-B-05 records the current build, Prisma and coverage gates; coverage remains failed only on the unchanged global 60% branch threshold.
- Security negative matrix: The coordinator authorizes family membership and write role before idempotency lookup or winner access. This real database assertion proves conflicting payload reuse cannot create coordinator facts or advance the revision. HTTP unauthenticated/non-member/viewer ingress tests remain mandatory before route-level behavior is claimed.
- Database/migration evidence: PASS-REAL on dedicated local PostgreSQL 18.1 at localhost:5433, database `homefinance_phase1_test`, 2026-08-31. First `createIncome` commits; reusing its key with a different amount returns `IDEMPOTENCY_KEY_REUSED` (409) and preserves the prior Income, IdempotencyRecord, AuditEvent and Family.cacheVersion counts/values.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: local Windows, PostgreSQL 18.1 dedicated non-production database, 2026-08-31; staging/release observation not performed
- Observed result: The database-arbitrated coordinator rejects a reused scoped key with a different canonical payload without modifying any financial, idempotency or audit fact and without invalidating a report through a revision bump. This is local PASS-REAL verification, not HTTP route adoption.
- Rollback rehearsal: No migration or production code changed in this evidence slice. Test-only rollback is `git revert` of its verification commit; the existing additive migration and forward-fix policy remains unchanged.
- ADR/project-memory/audit/Graphify updates: Tracker and project memory record the local PASS-REAL scope; semantic Graphify incremental refresh is required for this test/documentation change. Only reviewed source-backed P1-B-03 edges may be treated as EXTRACTED.
- Remaining risks: Income/Expense routes still bypass the coordinator; import/recurring/AI mutation adoption, populated upgrade/restore, Redis/Compose, staging and release observation remain unverified. P1-B-04's historical unclassified aggregate-run INTERNAL_ERROR remains AT_RISK. The global coverage gate remains below the unchanged branch threshold.
