# P1-E-06 Evidence

- Task ID: P1-E-06
- Evidence status: REGRESSION_VERIFIED / AT_RISK
- Baseline commit: `cdfcb67`
- Branch/commit: `codex/phase1-ledger-trust@6704300` plus proposal confirmation/history commits `cdfcb67`, `d945e65`, `c769a78`
- Source evidence: `backend/src/routes/aiProposalConfirmRoute.phase1.test.ts`, `backend/src/services/aiProposalConfirmationService.test.ts`, `backend/src/tests/aiProposalRoutes.phase1.integration.test.ts`, `frontend/src/pages/AIPage.test.tsx`
- Affected families/roles/routes/tables: all AI proposal creation/confirmation paths, family membership and writer role policy, proposal and ledger persistence
- RED command: `cd backend; npm test -- --runInBand src/routes/aiProposalConfirmRoute.phase1.test.ts src/services/aiProposalConfirmationService.test.ts`; `RUN_INTEGRATION=1 npx jest src/tests/aiProposalRoutes.phase1.integration.test.ts --runInBand --forceExit`
- RED failure summary: route schemas initially rejected server-owned proposal item IDs and the confirmation matrix did not prove malformed, tampered, replayed, cross-family or executed proposal behavior.
- GREEN scope: confirmation schemas accept only the approved proposal metadata plus final actions; route and service tests enforce authorization before proposal lookup/mutation, server-owned item mapping, canonical validation, conditional status/version claim, idempotency and stable DomainError mapping.
- Regression commands/results: real PostgreSQL AI route suite PASS, 17/17; backend AI route/service focused regression PASS, 52/52; frontend AI page/service focused regression PASS, 6/6. The real suite covers viewer/non-member, cross-family, expired, malformed amount/date/type, duplicate/unknown item, missing key, same-key changed payload, already executed proposal and concurrent confirmation behavior.
- Security negative matrix: all listed negative cases return denial/validation/conflict and leave prohibited ledger facts absent or unchanged. Successful same-key replay returns the original response; a different key after execution is rejected; the executed-history contract is read-only and does not re-open confirmation. Commit `6704300` additionally removes the former direct Prisma-backed `executeActions` export and implementation; the legacy `/execute-actions` URL remains present only as a strict compatibility adapter and is not claimed removed.
- Database/migration evidence: PASS-REAL on local PostgreSQL 18.1 at `localhost:5433`; no new schema change in this security-matrix slice. Redis was not running and produced known fail-open rate-limit logs; MinIO and browser/Compose paths were not verified.
- Frontend/E2E evidence: PASS-MOCK for page-level guards and history status; Playwright harness commit `9e89525` discovers 4 journeys, but `npm run test:e2e` is BLOCKED by unavailable Docker, so multi-tab and real browser negative journeys remain unobserved.
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: the supported proposal confirmation path has a regression-verified negative security matrix, but Phase 1 cannot be marked complete while the legacy compatibility bypass, external infrastructure and release gates remain open.
- Rollback rehearsal: preserve proposal and audit records; contain or disable the legacy adapter through a forward fix. Do not delete populated financial facts or proposal history as rollback.
- ADR/project-memory/audit/Graphify updates: tracker, project memory, ADR-0004 and HF-AI-001 are updated; semantic Graphify refresh remains pending.
- Remaining risks: legacy raw-action endpoint removal/containment, Balance action support, Redis/MinIO/Compose, browser E2E, release observation and global coverage threshold.
