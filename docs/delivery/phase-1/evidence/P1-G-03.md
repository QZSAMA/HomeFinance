# P1-G-03 Evidence

- Task ID: P1-G-03
- Evidence status: PASS-MOCK
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; current P1-G-03 ImportPage component-test slice is pending its atomic commit
- Source evidence: Phase 1 design §9/§11, `frontend/src/pages/ImportPage.tsx`, and `frontend/src/pages/ImportPage.test.tsx`
- Affected families/roles/routes/tables: Browser-side legacy import preview/confirmation UI for the selected `currentFamily`; no backend route, persistence, family authorization or financial-mutation protocol changed
- RED command: `cd frontend; npm test -- ImportPage.test.tsx`
- RED failure summary: On 2026-08-31 the failed-confirmation test found the visible `导入被拒绝` text but no `role="alert"`; the success test found `成功导入 1 条记录` but no `role="status"`; and the file-control test found a `CSV 文件` label with no associated form control. Each failure was reproduced before its respective implementation change.
- GREEN commit and scope: The current slice adds `ImportPage.test.tsx` with three React Testing Library contracts. `ImportPage` now exposes failed confirmation through `role="alert"`, successful confirmation through `role="status"`, and binds the CSV label to its file input with `htmlFor`/`id`. The pre-existing `importing` guard is asserted to disable the confirmation button and prevent a second client-side call while the first request is pending. No idempotency header, import API contract, or ledger behavior was added.
- REFACTOR summary: Interaction tests now select the file control by its accessible label instead of a CSS selector; shared service and Zustand boundaries remain mocked only at the network/store boundary.
- Regression commands/results: RED commands above failed for the stated missing semantics. GREEN: `npm test -- ImportPage.test.tsx` passes 1 suite / 3 tests. Full frontend `npm test` passes 2 suites / 8 tests; `npm run lint` exits 0 with the pre-existing 16 warnings and no warnings in changed files; `npm run build` passes, retaining the pre-existing 856.18 kB minified main-chunk warning.
- Security negative matrix: The component receives the currently selected family from the common family store and confirmation remains disabled while pending, preventing a local double-click replay. This is only PASS-MOCK: unauthenticated/non-member/viewer rejection, server-side idempotency, cross-family enforcement, malformed input and concurrent HTTP replay remain backend contracts pending P1-C/P1-G-02.
- Database/migration evidence: NOT_RUN unless task is design-only
- Frontend/E2E evidence: PASS-MOCK via jsdom/React Testing Library; Playwright and Compose evidence remain NOT_RUN
- Release environment/time: N/A
- Observed result: The import confirmation UI now presents success/failure semantics to assistive technology and has an executable local-replay guard. This does not change or prove the legacy server's partial-success and non-idempotent import behavior.
- Rollback rehearsal: Revert the presentation attributes and component test together; no persisted data, network contract or migration is involved
- ADR/project-memory/audit/Graphify updates: Tracker and project memory record this PASS-MOCK slice. A semantic Graphify incremental refresh must replace only the changed source provenance and preserve reviewed source-backed edges from unaffected files.
- Remaining risks: P1-G-03 remains `GREEN_MINIMAL`/AT_RISK: coverage currently reaches only ImportPage, not all mutation entries; browser E2E, role matrix, stable idempotency-key replay and server-side import atomicity remain open.
