# P1-G-03 Evidence

- Task ID: P1-G-03
- Evidence status: PASS-MOCK
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust@5a564c9; current slices cover Import, text/OCR AI confirmation presentation, and Recurring occurrence retry identity
- Source evidence: Phase 1 design sections 9/11, Import/AI page tests, frontend/src/services/recurringService.test.ts, and frontend/src/pages/RecurringPage.test.tsx
- Affected families/roles/routes/tables: browser-side mutation UI for the selected currentFamily; Import sends the server-owned batch contract, Recurring sends an explicit occurrence and stable key, while backend authorization and persistence remain server-owned
- RED commands: cd frontend; npm test -- src/pages/ImportPage.test.tsx; cd frontend; npm test -- src/pages/AIPage.test.tsx; and recurring focused service/page tests before occurrence/key implementation
- RED failure summary: Import lacked alert/status semantics and an associated file label; AI lacked alert/status semantics; Recurring execute sent neither scheduledFor nor an Idempotency-Key and could not reuse one request identity after a failed retry
- GREEN scope: Import exposes accessible failure/success/pending behavior and uses the server-owned batch/hash plus stable-key contract from P1-C-06. AI confirmation exposes accessible failure/success/pending behavior, and backend commit 5a564c9 now sends text actions through the same proposedActions UI without pre-confirmation execution. Recurring sends scheduledFor plus a stable retry key. Server-owned AIProposal persistence and atomic confirmation remain P1-E work
- REFACTOR summary: interaction tests select controls by accessible label/role; service/store/network boundaries remain mocked. Stable keys are retained only for the same local operation and are cleared after successful completion or replacement input
- Regression commands/results: Import and AI focused tests remain PASS. Recurring focused service/page tests PASS, 2 files / 2 tests. Full frontend npm test passes 6 files / 15 tests; npm run lint exits 0 with 16 pre-existing warnings; npm run build passes, retaining the known 857.88 kB minified main-chunk warning
- Security negative matrix: components receive the selected family from the common store; pending guards reduce local duplicate clicks, and Import/Recurring retain stable keys across failed retries. Text AI now has a backend PASS-MOCK assertion that executeActions is not called before confirmation. AI proposal persistence, tamper/version checks, and exactly-once confirmation remain P1-E work
- Database/migration evidence: frontend tests do not establish persistence; Import and Recurring PostgreSQL evidence is recorded under P1-C and P1-D
- Frontend/E2E evidence: PASS-MOCK via jsdom/React Testing Library; Playwright, page-reload recovery, multi-tab behavior, and Compose evidence remain NOT_RUN
- Release environment/time: local Windows, 2026-09-01; release observation NOT_RUN
- Observed result: Import and AI confirmation UIs expose executable success/failure/pending semantics; text and OCR suggestions now both enter the AI confirmation UI. Import and Recurring have stable request-identity contracts, while AI still uses a legacy raw-actions confirmation endpoint pending P1-E-02~06
- Rollback rehearsal: revert each page/service contract with its focused tests; do not keep a frontend that sends an occurrence/hash contract rejected by the matching backend
- ADR/project-memory/audit/Graphify updates: tracker and project memory record the expanded PASS-MOCK slice. Semantic Graphify refresh remains pending because the installed AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: P1-G-03 remains GREEN_MINIMAL / AT_RISK: browser E2E, reload/multi-tab recovery, stable AI confirmation keys, server-owned proposal state, and proposal cancellation/edit replay remain open
