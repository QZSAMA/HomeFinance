# P1-G-03 Evidence

- Task ID: P1-G-03
- Evidence status: PASS-MOCK
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; current P1-G-03 ImportPage and AIPage component-test slices are pending their atomic commit
- Source evidence: Phase 1 design §9/§11, `frontend/src/pages/ImportPage.tsx`, `frontend/src/pages/ImportPage.test.tsx`, `frontend/src/pages/AIPage.tsx`, and `frontend/src/pages/AIPage.test.tsx`
- Affected families/roles/routes/tables: Browser-side legacy import and AI confirmation UI for the selected `currentFamily`; no backend route, persistence, family authorization or financial-mutation protocol changed
- RED commands: `cd frontend; npm test -- src/pages/ImportPage.test.tsx` and `cd frontend; npm test -- src/pages/AIPage.test.tsx`
- RED failure summary: On 2026-08-31 the import failed-confirmation test found the visible `导入被拒绝` text but no `role="alert"`; the success test found `成功导入 1 条记录` but no `role="status"`; and the file-control test found a `CSV 文件` label with no associated form control. The AI confirmation test then found no `role="alert"` after `确认被拒绝`; after that minimum fix, the pending-confirmation test found no `role="status"` after a successful result. Each failure was reproduced before its respective implementation change.
- GREEN scope: `ImportPage.test.tsx` has three React Testing Library contracts. `ImportPage` exposes failed confirmation through `role="alert"`, successful confirmation through `role="status"`, and binds the CSV label to its file input with `htmlFor`/`id`; its pre-existing `importing` guard is asserted to prevent a second client-side call while pending. `AIPage.test.tsx` adds two contracts: a rejected proposed-action confirmation is rendered as an in-page `role="alert"` and can be retried, while a pending confirmation disables its button, prevents a second local call, and reports the returned successful-action count with `role="status"`. No idempotency header, import/AI API contract, ledger behavior, or proposal-only server architecture was added.
- REFACTOR summary: Interaction tests select import controls by accessible label and AI controls by accessible roles; shared AI/import service and Zustand boundaries remain mocked only at the network/store boundary.
- Regression commands/results: RED commands above failed for the stated missing semantics. GREEN: `npm test -- src/pages/ImportPage.test.tsx` passes 1 suite / 3 tests; `npm test -- src/pages/AIPage.test.tsx` passes 1 suite / 2 tests. Full frontend `npm test` passes 3 suites / 10 tests; `npm run lint` exits 0 with 16 pre-existing warnings (including the pre-existing `AIPage` Hook-dependency warning); `npm run build` passes, retaining the known 856.71 kB minified main-chunk warning.
- Security negative matrix: The components receive the currently selected family from the common family store and confirmation remains disabled while pending, preventing a local double-click replay. This is only PASS-MOCK: unauthenticated/non-member/viewer rejection, server-side idempotency, cross-family enforcement, malformed input and concurrent HTTP replay remain backend contracts pending P1-C/P1-G-02; AI proposal persistence and exactly-once confirmation remain blocked P1-E work.
- Database/migration evidence: NOT_RUN unless task is design-only
- Frontend/E2E evidence: PASS-MOCK via jsdom/React Testing Library; Playwright and Compose evidence remain NOT_RUN
- Release environment/time: N/A
- Observed result: Import and AI proposed-action confirmation UIs now present success/failure semantics to assistive technology and have executable local-replay guards. The AI success message reports only the number of successful results returned by the legacy API. This does not change or prove legacy server-side import/AI atomicity, authorization, or idempotency.
- Rollback rehearsal: Revert the presentation attributes and component test together; no persisted data, network contract or migration is involved
- ADR/project-memory/audit/Graphify updates: Tracker and project memory record this PASS-MOCK slice. A semantic Graphify incremental refresh must replace only the changed source provenance and preserve reviewed source-backed edges from unaffected files.
- Remaining risks: P1-G-03 remains `GREEN_MINIMAL`/AT_RISK: coverage reaches only two mutation entries, not all mutation paths; browser E2E, role matrix, stable idempotency-key replay, server-side import atomicity, and AI proposal-only confirmation remain open.
