# P1-E-07 AI Asset/Liability Balance mutation

## Status

- State: REGRESSION_VERIFIED
- Health: AT_RISK
- Evidence: PASS-MOCK + PASS-REAL
- Branch: `codex/phase1-ledger-trust`
- Baseline: `87bf08d`
- Implementing commits: `fc50633`, `d204201`, `bdabe7b`
- Plan: `docs/superpowers/plans/2026-09-01-ai-balance-mutation.md`

## Scope

This slice connects confirmed AI `create_asset` and `create_liability` actions to a transaction-scoped Balance Mutation Service. The existing proposal-level `CONFIRM_AI_PROPOSAL` coordinator remains the only idempotency and PostgreSQL transaction boundary. Ordinary Asset/Liability HTTP CRUD, balance queries, valuation, currency conversion, and external infrastructure are explicitly out of scope.

## Acceptance contract

- A member/admin can confirm a server-owned Asset or Liability proposal only after the proposal item type and edited action type match.
- The balance fact, proposal claim/completion, item result, AuditEvent, and IdempotencyRecord commit atomically.
- Same-key confirmation replays the stored result without a second balance fact.
- Viewer, non-member, malformed/tampered, expired, and transaction-failure paths leave balance tables and proposal state unchanged.
- Asset `value` and Liability `amount` are finite non-negative values; currency is normalized to a three-letter uppercase code.

## Evidence log

- RED: the focused confirmation test initially failed with `AI_BALANCE_MUTATION_UNAVAILABLE`; the adapter and Balance service focused REDs likewise failed because the transaction protocol did not expose Asset/Liability writers. These were target-behavior failures, not environment failures.
- GREEN: `balanceMutationService` validates scope, finite non-negative amounts, optional dates and three-letter currencies, then writes only through the supplied transaction client. The confirmation coordinator now dispatches matching `create_asset`/`create_liability` proposal items through that service. Focused Balance/AI unit and route regression is 18/18.
- Real PostgreSQL: local PostgreSQL 18.1 at `localhost:5433` passes the AI confirmation and route suites, 25/25. The full integration run passes 11 suites / 76 tests. It verifies atomic Asset/Liability facts plus proposal item results, AuditEvent and IdempotencyRecord, same-key replay without a second balance fact, concurrent proposal claim arbitration and injected transaction rollback with zero balance/proposal metadata residue.
- Full backend behavior: `npm test -- --runInBand` passes 50 suites / 411 tests. The required coverage command executes all assertions and passes the unchanged 60% global threshold: statements 75.61%, branches 60.14%, functions 71.42%, and lines 76.40%. The threshold was not lowered.
- Security/validation: the proposal confirmation matrix continues to cover viewer/non-member, expiry, tamper, replay and concurrency behavior; Balance-specific tests cover proposal-item type matching and malformed/negative balance input before transaction writes. Ordinary Asset/Liability HTTP CRUD is not part of this slice.
- Docker/Compose/Playwright: blocked because Docker CLI is unavailable on this host; no PASS-E2E or external Redis/MinIO claim is made.

## Remaining risks

- Existing Asset/Liability routes still contain direct Prisma mutations and route-local access helpers; P1-A-07 remains AT_RISK.
- The legacy `/ai/execute-actions` URL remains a strict compatibility adapter and requires release-removal observation.
- Balance query/valuation, currency conversion and ordinary balance CRUD remain outside this slice and require separate route/service adoption work.
- Ordinary Asset/Liability HTTP CRUD remains outside this slice. Populated upgrade/restore, Redis/MinIO/Compose verification, browser E2E, release observation, and semantic Graphify refresh remain open.
