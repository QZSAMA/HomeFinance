# P1-C-05 Evidence

- Task ID: P1-C-05
- Evidence status: PASS-REAL for local PostgreSQL retry/replay/concurrency contracts
- Baseline commit: `66970be` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@66970be`
- Source evidence: `backend/src/tests/importConfirm.phase1.integration.test.ts`, `backend/src/routes/import.ts`, `backend/src/services/importService.ts`, and `docs/adr/0003-atomic-import-batches.md`
- Affected families/roles/routes/tables: authenticated family member/admin batch confirmation; `ImportBatch` claim and all Income/Expense writes share one PostgreSQL transaction; batch-level and row-level idempotency records are scoped to family/actor/operation
- RED command: The initial C04 batch-only route test failed because the route still required client `items`; the completed integration contract was then run with `RUN_INTEGRATION=1 npx jest src/tests/importConfirm.phase1.integration.test.ts --runInBand --forceExit`
- RED failure summary: before the implementation, valid batch-only requests returned 400 and no batch submission occurred; this gate is now verified after the route migration
- GREEN/verification scope: same idempotency key replays the stored response with `Idempotency-Replayed: true` and no second ledger row; a committed batch with a different key returns `IMPORT_BATCH_NOT_CONFIRMABLE`; two concurrent confirmations produce one 200 and one 409, with one Income and one Expense total
- Regression commands/results: `npm run test:integration` PASS, 7 suites / 41 tests; focused import/route/service/CORS/schema tests PASS, 5 suites / 24 tests; role-method matrix PASS-REAL; `npm run build` PASS
- Security negative matrix: invalid server row, hash tamper, expiry, cross-family batch and client-owned item attempts return stable rejection and zero new ledger facts; middleware still denies unauthenticated/viewer/unknown-role/non-member requests before the handler
- Database/migration evidence: PostgreSQL 18.1 at `localhost:5433/homefinance_phase1_test`; assertions cover `PREVIEWED → COMMITTED`, row result references, idempotency replay, and concurrent claim arbitration
- Frontend/E2E evidence: frontend still sends the legacy `items` payload and therefore must be migrated under P1-C-06; browser E2E NOT_RUN
- Release environment/time: local Windows, 2026-09-01; Redis/MinIO/Compose/staging/release observation NOT_RUN
- Observed result: `REGRESSION_VERIFIED / AT_RISK`; local PG gate is open, while infrastructure/release and frontend contract gates remain open
- Rollback rehearsal: not run. Disable batch confirm or restore the last verified commit while retaining batch/row and audit/idempotency facts; do not restore partial-success client-owned confirmation
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable; tracker and project memory updated. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: distinct-key concurrent losers currently receive a stable 409 rather than the winner response; broader retry policy, upgrade/restore, expired-batch cleanup, frontend refresh recovery, E2E and release observation remain open
