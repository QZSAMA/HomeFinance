# P1-C-06 Evidence

- Task ID: P1-C-06
- Evidence status: PASS-MOCK for frontend batch confirmation contract
- Baseline commit: `66970be` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@50530b5`
- Source evidence: `frontend/src/services/importService.ts`, `frontend/src/services/importService.test.ts`, `frontend/src/pages/ImportPage.tsx`, and `frontend/src/pages/ImportPage.test.tsx`
- Affected families/roles/routes/tables: `ImportPage` for the selected family; preview reads server-owned batch/hash headers, and confirm calls `/families/:familyId/import/confirm` with batch/hash/category patch and `Idempotency-Key`
- RED command: `npm test -- --run src/services/importService.test.ts src/pages/ImportPage.test.tsx`; observed 3 failures because preview still returned an array, confirm still sent client items, and the page could not render the new metadata-backed confirm action
- RED failure summary: service returned the raw array instead of `{items,batchId,previewHash}`; the confirm call body was `{items: 'batch-1'}` rather than the batch contract; the page did not expose a confirm button for server metadata
- GREEN scope: service validates and returns lowercase 64-character preview hash plus batch ID from response headers; page stores batch/hash, displays batch identity, sends only row-number keyed category patches, creates one stable request key per confirmation attempt, retains it on failure, and clears batch state only after success or a new file selection
- REFACTOR summary: preserved existing loading/error/status UI and local duplicate-click protection; kept the legacy array response adapter at the service boundary so backend response compatibility remains intact
- Regression commands/results: focused service/page tests PASS, 2 files / 6 tests; full frontend tests PASS, 4 files / 13 tests; `npm run lint` PASS with 16 pre-existing warnings; `npm run build` PASS with the known oversized 857.59 kB main chunk warning
- Security negative matrix: client financial fields are not sent by the migrated service/page; backend remains responsible for family authorization, hash validation, canonical row validation and atomic mutation. Browser E2E NOT_RUN
- Database/migration evidence: no frontend persistence or migration changed; backend PostgreSQL evidence is recorded in P1-C-04/P1-C-05
- Frontend/E2E evidence: PASS-MOCK component/service contracts; refresh recovery, browser console, full import journey and network-level E2E remain open
- Release environment/time: local Windows, 2026-09-01; staging/release observation NOT_RUN
- Observed result: `REGRESSION_VERIFIED / AT_RISK`; frontend contract aligns with backend batch-only confirm, but E2E/release and broader UI warning/bundle gates remain open
- Rollback rehearsal: not run. Revert the frontend commit while disabling the new backend-only confirm route if rollback is required; do not re-enable untrusted client-owned financial fields against the atomic endpoint
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable; tracker and project memory updated. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph
- Remaining risks: the component does not restore an in-progress batch after a full page reload, expired preview cleanup is not implemented, and E2E plus release observation remain open
