# P1-B-04 Evidence

- Task ID: P1-B-04
- Evidence status: PASS-REAL
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; persistence foundation `17c2644`; coordinator arbitration implementation `63ee373`; `18bb56d` carries the adjacent P1-A-03 hardening, and this change set includes raw-Prisma-cause capture
- Source evidence: Phase 1 design §4.2/§9, implementation plan Task 6, ADR-0002
- Affected families/roles/routes/tables: family-scoped LedgerApplicationService, IdempotencyRecord, AuditEvent and Income; member/admin mutation only, viewer/non-member negative tests mandatory
- RED command: `$env:RUN_INTEGRATION='1'; $env:DATABASE_URL='postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public'; npx jest src/tests/phase1-concurrency.integration.test.ts --runInBand --detectOpenHandles`
- RED failure summary: real PostgreSQL first exposed two defects: Prisma composite `P2002.meta.target` fields were not recognized as the scoped unique conflict, and adapter JSON conversion corrupted Date/null values during response replay. A focused unit RED reproduced the composite-field failure before the fix. On 2026-08-31, the raw-error follow-up RED verified that an unmapped simulated Prisma `P2028` became `INTERNAL_ERROR` with no retained cause.
- GREEN scope: `financialMutationCoordinator` now treats both named-index and composite-field scoped `P2002` metadata as database arbitration, authorizes before root winner lookup, replays completed winners, returns `IDEMPOTENCY_KEY_REUSED` for hash mismatch, and bounds incomplete-winner polling with retryable `IDEMPOTENCY_IN_PROGRESS`. `prismaFinancialMutationStore.ts` provides a typed root/transaction adapter and preserves JSON Date/null semantics. `mapPrismaError` now stores the original error as the non-enumerable `cause` of its stable `DomainError`; no Prisma message, metadata, or raw code is added to the client-facing error contract.
- REFACTOR summary: extracted typed Prisma adapter and bounded winner polling; the mapper reuses the standard Error-cause pattern without changing stable status/code semantics. Focused coordinator/orchestration tests pass (20/20), and four default-suite Prisma adapter contract tests protect JSON serialization, malformed-value rejection, decimal conversion and root snapshot operation validation.
- Regression commands/results: historical verification passed `npm run test:integration`, 3 suites / 24 tests; `npm run build`; `npx prisma validate`; `npx prisma format --check`; and default Jest, 31 suites / 267 tests. On 2026-08-31, the first aggregate integration run produced one `INTERNAL_ERROR` during the 20-request coalescing assertion. An immediate full rerun passed 3 suites / 24 tests and five isolated repetitions of `phase1-concurrency.integration.test.ts` passed. The raw-error RED/GREEN now verifies a simulated `P2028` is available through the non-enumerable `DomainError.cause` before callers inspect the mapped error. The current full integration suite again passes 3 suites / 24 tests, `npm run build`, `npx prisma validate`, and `npx prisma format --check` pass. The required coverage command now executes 33 suites / 273 tests with statements 62.14%, branches 45.18%, functions 62.18%, and lines 62.58%; the unchanged 60% branch threshold still fails. Existing OCR/MinIO test warnings remain.
- Security negative matrix: member/admin authorization remains checked inside the transaction and before root winner lookup; same-key/different-hash zero-write behavior and stale-version predicate passed in real PostgreSQL. Unauthenticated/non-member HTTP route coverage remains pending route adoption.
- Database/migration evidence: PostgreSQL 18.1 at localhost:5433, database `homefinance_phase1_test`; 20 identical requests produced one committed mutation and 19 replays sharing operation/resource IDs, with one Income, one IdempotencyRecord and one AuditEvent. Same-key/different-payload, response-loss replay, and stale-version competition also passed.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: local Windows, PostgreSQL 18.1 dedicated non-production database, 2026-08-28; staging/release observation not performed
- Observed result: P1-B-04 acceptance behavior has historical `REGRESSION_VERIFIED`/`PASS-REAL` local evidence, but its health is `AT_RISK` after one non-reproducible 2026-08-31 aggregate-run failure. A future recurrence now preserves the original error object and Prisma code for test or server-side diagnostic handling without widening the client contract. Route adoption, actual failure classification, populated upgrade/restore, staging and release observation remain open.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: coordinator arbitration behavior and real integration evidence are recorded. A narrow Graphify update refreshed code structure and added only reviewed, source-backed `EXTRACTED` edges for the non-enumerable raw-error cause and the still-unclassified failure risk; no inferred or unverified edges were merged.
- Remaining risks: Income/Expense routes still bypass the coordinator; import/recurring/AI mutation adoption, revision consistency, populated upgrade/restore, staging and release observation remain unverified. Classify the original production-like failure if it recurs and add a deterministic regression for its actual code; coverage gate remains below the unchanged global threshold.
