# P1-B-04 Evidence

- Task ID: P1-B-04
- Evidence status: BLOCKED (RED captured; real PostgreSQL credentials unavailable)
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; persistence foundation `17c2644`; coordinator arbitration adapter implemented in working tree
- Source evidence: Phase 1 design §4.2/§9, implementation plan Task 6, ADR-0002
- Affected families/roles/routes/tables: family-scoped LedgerApplicationService, IdempotencyRecord, AuditEvent and Income; member/admin mutation only, viewer/non-member negative tests mandatory
- RED command: `$env:RUN_INTEGRATION='1'; $env:DATABASE_URL='postgresql://postgres:postgres@localhost:5432/family_finance?schema=public'; npx jest src/tests/phase1-concurrency.integration.test.ts --runInBand --detectOpenHandles`
- RED failure summary: Suite reached `beforeAll` and failed with Prisma `P1000` authentication failure; no false concurrency pass is claimed.
- GREEN scope: `financialMutationCoordinator` now treats scoped `P2002` as database arbitration, authorizes before root winner lookup, replays completed winner, returns `IDEMPOTENCY_KEY_REUSED` for hash mismatch, and bounded `IDEMPOTENCY_IN_PROGRESS` for incomplete winner. `prismaFinancialMutationStore.ts` provides a typed root/transaction adapter.
- REFACTOR summary: Extracted typed Prisma adapter and bounded winner polling; focused coordinator tests pass (11/11).
- Regression commands/results: `npm run build` PASS; focused coordinator + ledger service tests PASS (15/15). Full integration remains BLOCKED by database authentication.
- Security negative matrix: DESIGNED where applicable; execution evidence not yet available
- Database/migration evidence: PostgreSQL 18.1/scoped unique primitive are available through `17c2644`; this environment rejected supplied credentials, so 20-way coordinator execution and loser replay remain unobserved.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: local PostgreSQL endpoint reachable but credentials unavailable on 2026-08-28; no release observation
- Observed result: lifecycle remains READY/BLOCKED pending valid PostgreSQL credentials; no P1-B-04 PASS-REAL claim exists
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: coordinator arbitration behavior and environment limitation recorded; Graphify refresh remains pending after commit
- Remaining risks: real PostgreSQL concurrency, response-loss replay, same-key/different-hash zero-write behavior, route adoption, and revision consistency remain unverified; coverage gate is still below threshold per baseline.
