# P1-A-01 Evidence

- Task ID: P1-A-01
- Evidence status: PASS-MOCK for the route-level negative matrix; PASS-REAL for authenticated-member Income/Expense CRUD HTTP-to-PostgreSQL contracts
- Baseline commit: `4bd2609` on `codex/phase1-ledger-trust`
- Source evidence: `backend/src/routes/incomes.phase1.test.ts`, `backend/src/routes/expenses.phase1.test.ts`, `backend/src/tests/ledgerRoutes.phase1.integration.test.ts`, and Phase 1 design §6/§7
- Affected families/roles/routes/tables: Income/Expense read/write routes; `familyId` remains the tenant boundary; member/admin may mutate and viewer/non-member paths are rejected before Ledger or Prisma writes
- RED command: `npm test -- --runInBand src/routes/incomes.phase1.test.ts src/routes/expenses.phase1.test.ts`
- RED failure summary: Before adoption, the original two route suites had 8 expected failures because POST/PUT/DELETE did not call Ledger and retained direct Prisma write paths. Follow-on REDs showed missing `Idempotency-Replayed` and incomplete Zod validation error contracts.
- GREEN scope: 16 route-adapter contracts verify create/update/delete delegation, `If-Match` command propagation, compatible enriched responses, replay signalling, malformed-input zero side effects, stable stale-version errors, and viewer/non-member denial.
- REFACTOR summary: Shared HTTP-only concerns are centralized in `backend/src/routes/ledgerRouteSupport.ts`; the service boundary and centralized family policy remain outside the route adapters.
- Regression commands/results: focused route/security/cache suite PASS, 4 suites / 18 tests; expanded focused backend suite PASS, 5 suites / 45 tests; `npm run build`, `npm run test:integration` (4 suites / 29 tests), `npx prisma validate`, and `npx prisma format --check` PASS. The full coverage run executes 37 suites / 318 tests with all assertions passing, but correctly exits non-zero because global branch coverage is 49.91% against the unchanged 60% threshold (statements 68.13%, functions 66.37%, lines 68.64%).
- Security negative matrix: PASS-MOCK for viewer/non-member and malformed-input zero side-effect cases in the changed routes; existing family permission and middleware suites also pass. Unauthenticated and full route-by-route real-database matrices remain open.
- Database/migration evidence: no schema or migration change. `DATABASE_URL=...localhost:5433/homefinance_phase1_test` integration suite PASS, 4 suites / 29 tests. `ledgerRoutes.phase1.integration.test.ts` is PASS-REAL for authenticated-member HTTP routes: Income create, same-key replay, weak-ETag update, stale 409 and delete; Expense create, update, stale 409 and delete.
- Frontend/E2E evidence: no frontend behavior changed. Lint exits 0 with 16 pre-existing warnings; production build passes with the known 856.71 kB main chunk warning. Browser E2E remains NOT_RUN.
- Release environment/time: local Windows, 2026-08-31; no staging or release observation.
- Observed result: route contracts are regression-protected with a real authenticated-member CRUD persistence proof, but this task remains REFACTORED / AT_RISK until global coverage and an independent full role/method security review are complete.
- Rollback rehearsal: not run. The Phase 1 adapter rollback is a single-entry-point forward fix/revert with no dual write; persisted idempotency/audit facts are retained.
- ADR/project-memory/audit/Graphify updates: ADR-0001/0002 remain applicable. Project memory and tracker are updated in this change. Semantic Graphify refresh is pending: the installed AST-only updater would overwrite the reviewed semantic graph and was intentionally not run.
- Remaining risks: global branch coverage gate, complete unauthenticated/non-member/viewer real-database role matrix, P1-B-04 historic unclassified `INTERNAL_ERROR`, populated upgrade/restore, Redis/MinIO/Compose, browser E2E, staging/release observation, and semantic Graphify tooling.
