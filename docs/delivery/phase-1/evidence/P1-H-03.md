# P1-H-03 Evidence

- Task ID: P1-H-03
- Evidence status: `PASS-REAL (REHEARSAL) + BLOCKED (STAGING)`
- Lifecycle state: `BLOCKED`; health: `AT_RISK`
- Baseline commit: `6e606e3` (P1-G-05 infrastructure-recovery branch)
- Branch/commit: [`codex/p1-h03-release-rehearsal`](https://github.com/QZSAMA/HomeFinance/tree/codex/p1-h03-release-rehearsal) @ `c03d35ec9bcfd3adbb50fbd57c45043abf33d0f0`
- Draft PR: [#6 P1-H-03 populated release rehearsal](https://github.com/QZSAMA/HomeFinance/pull/6) (open, Draft; not merged)
- Scope: disposable populated PostgreSQL migration/backup/restore/forward-recovery rehearsal; no production or staging data was used

## RED → GREEN

- Focused RED: `node --test e2e/release-rehearsal/run.test.mjs` initially failed while the staged runner/modules/workflow/runbook contracts were absent (`ERR_MODULE_NOT_FOUND`/`ENOENT`), as required by the implementation plan.
- Focused GREEN: the same command on the final branch passed `18/18` tests; `node --check e2e/release-rehearsal/run.mjs`, checkpoint discovery (`--list`, exactly nine names), and `git diff --check` also passed.
- Implementation/evidence commits: `64b4674`, `f32c3f3`, `2c682b9`, `a99fa44`, `1b56634`, `ad38b67`, `c03d35e`.

## Real Actions execution

All four runs below used the same branch SHA `c03d35ec9bcfd3adbb50fbd57c45043abf33d0f0` and completed successfully on 2026-09-08:

| Gate | Run |
|---|---|
| CI | [34202444766](https://github.com/QZSAMA/HomeFinance/actions/runs/34202444766) |
| Browser E2E | [34202444921](https://github.com/QZSAMA/HomeFinance/actions/runs/34202444921) |
| Infrastructure Recovery | [34202444902](https://github.com/QZSAMA/HomeFinance/actions/runs/34202444902) |
| Populated Release Rehearsal | [34202444770](https://github.com/QZSAMA/HomeFinance/actions/runs/34202444770) |

Runner evidence: GitHub-hosted `ubuntu-24.04` image (`Ubuntu 24.04.4`, image release `20260831.293.1`), Node `v20.20.2` / npm `10.8.2`, PostgreSQL `16.15`. The rehearsal workflow had `contents: read`, a 30-minute timeout, and unconditional Compose teardown.

## Migration and backup facts

- Legacy migration cut: `20260723030000_add_file_id_to_ai_conversation`.
- Current migration head: `20260903100200_add_goal_contributions`.
- `pre-upgrade.dump`: `33,011` bytes; SHA-256 `467cad823ff3b61cb4c9d1dd485e0e37c3d7d7dfab4a06355d623c2fa094feab`.
- `current.dump`: `78,930` bytes; SHA-256 `2e971287da4459e39852e62368196639c07807aa7e861f992eb4b5c9c887d1f7`.
- Both custom-format dumps were non-empty and passed `pg_restore --list`; dump content and credentials were not uploaded or persisted in Git.

## Checkpoints

The runner emitted all nine `PASS` lines. Timings are the observed milliseconds from run `34202444770`:

| Checkpoint | Result | Duration |
|---|---:|---:|
| migration-inventory-and-empty-target | PASS | 23,173 ms |
| legacy-schema-and-populated-fixture | PASS | 1,894 ms |
| pre-upgrade-backup-validated | PASS | 492 ms |
| populated-upgrade-preserves-legacy-facts | PASS | 3,918 ms |
| current-application-smoke-and-population | PASS | 47,910 ms |
| current-backup-validated | PASS | 10,803 ms |
| failed-release-restore | PASS | 30,027 ms |
| forward-recovery-from-pre-upgrade-backup | PASS | 22,571 ms |
| idempotent-migrate-and-final-cleanup | PASS | 23,769 ms |

The legacy fixture contained two families with isolated rows. Upgrade preserved the legacy manifest, required defaults/triggers/constraints, and zero orphan rows. The failed-release checkpoint injected `rehearsal_failure_sentinel` and `CORRUPTED-BY-REHEARSAL`, restored `current.dump`, and verified sentinel absence, original family description, exact current-manifest equality, and zero orphans. Forward recovery restored `pre-upgrade.dump`, deployed the current migrations, reran the smoke workload, and verified the upgraded manifest and zero orphans. The second migration deploy reported `No pending migrations to apply`. The runner summary reported `cleanup: true`; workflow teardown removed the Compose project with `down --volumes --remove-orphans`.

## Application smoke and safety matrix

The current production API paths were exercised using generated records (no direct database writes):

- Unauthenticated income-statement read: `401`.
- Authenticated non-member income-statement read: `403`.
- Viewer expense write: `403`, followed by a family-scoped read proving zero denied-row side effects.
- Family A/B isolation: Family B responses contained none of Family A IDs, descriptions, or generated ledger IDs.
- Income create with `Idempotency-Key`: first request created one row; identical replay returned the same operation/result and did not duplicate the row.
- CSV Import preview/confirm: one server-owned batch and exactly one expense row.
- Recurring rule execution: one execution and one linked ledger entry.
- AI chat returned a proposal only (`actions: []`); explicit version/hash confirmation committed exactly one expense and replay metadata.
- GoalContribution create and identical replay produced one allocation.
- Reconciliation: Income `120` − Expense `113` = Net income `7` in CNY.

## Limits and remaining risk

This is real disposable CI rehearsal evidence, not a release observation. It did not use protected GitHub `staging`, production/staging data, immutable backend/frontend image digests, PostgreSQL/MinIO production backup policy, observability access, or Release/Repository Owner approval. It does not establish production RPO/RTO, durable cross-system outbox or multi-file atomicity, historical Balance valuation/FX semantics, or production availability. P1-H-03 therefore remains `BLOCKED`/`AT_RISK`; it is not `RELEASED` or `OBSERVED`. P1-H-04 remains `BACKLOG`.

## Required next action

Provision the protected GitHub Environment `staging` with a required reviewer, approved populated/anonymized fixture, immutable image digests, coordinated PostgreSQL/MinIO backup scope, secrets/deployment permissions, observability access, and named Release/Repository Owners. Then execute the staging release, rollback/restore and forward-fix sequence in [`docs/runbooks/staging-release-and-recovery.md`](../../../runbooks/staging-release-and-recovery.md), collect 30 one-minute observation samples, and update this card only after the stop/go evidence is complete.

## Cross-document updates

- `docs/delivery/phase-1/phase-1-tracker.md`: P1-H-03 remains `BLOCKED`/`AT_RISK`; P1-H-04 remains `BACKLOG`.
- `docs/project-memory.md` and `docs/audit/2026-08-27-homefinance-deep-audit-report.md`: rehearsal facts and boundaries appended; production readiness is not claimed.
- Semantic Graphify refresh remains pending because the installed updater is AST-only; `graphify-out/` was intentionally not changed.
