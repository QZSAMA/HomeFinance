# P1-H-02 Evidence

- Task ID: P1-H-02
- Evidence status: IN_REVIEW / AT_RISK
- Baseline commit: `081084a`
- Branch/commit: `codex/phase1-ledger-trust@c644d50` for the current source and code-derived graph snapshot
- Source evidence: `graphify-out/graph.json`, `graphify-out/GRAPH_REPORT.md`, `docs/project-memory.md`, and the Phase 1 tracker
- Affected scope: family authorization, Ledger/Balance mutation services, Asset/Liability routes, evidence cards, ADRs and project memory; Graphify is navigation memory and not the executable source of truth
- Observed graph snapshot: 827 nodes, 822 edges, 194 communities; 91% EXTRACTED, 9% INFERRED, 0% AMBIGUOUS. Reviewed navigation hubs include `Current Family Data Scope`, `createAssetInTransaction()`, `createLiabilityInTransaction()`, `coordinateFinancialMutation()` and `Financial Write Integrity God Node`.
- RED evidence: documentation and architecture changes require an incremental semantic Graphify update. The available local updater is AST-only for the affected code/doc mix and can overwrite the reviewed semantic graph with a lower-fidelity structure; therefore the semantic refresh has not been executed.
- GREEN scope: no generated graph file was manually edited or replaced. The current reviewed graph is retained, and the tracker/evidence cards explicitly mark the semantic refresh as pending instead of upgrading inferred edges to facts.
- Regression commands/results: `git diff --check` passes for the governance changes; graph snapshot was read-only inspected. A semantic `detect_incremental`/merge run is NOT_RUN because the available runner cannot preserve the reviewed semantic extraction contract.
- Security negative matrix: not applicable to graph generation itself; all security behavior claims remain sourced from executable route/service tests and their evidence cards.
- Database/migration evidence: not applicable; no schema or production code changed in this card.
- Frontend/E2E evidence: not applicable; no UI behavior changed in this card.
- Release environment/time: local Windows, 2026-09-01; release observation NOT_RUN
- Observed result: graph navigation remains available and current code/domain hubs are discoverable, but P1-H-02 cannot advance to REGRESSION_VERIFIED until a semantic incremental runner is available and its EXTRACTED/INFERRED diff is reviewed.
- Rollback rehearsal: no graph rollback performed. If a future refresh degrades the graph, restore the reviewed `graphify-out` snapshot and rerun with a semantic-preserving workflow; never hand-edit generated nodes/edges.
- ADR/project-memory/audit/Graphify updates: tracker and this evidence card record the pending boundary; the code and Asset evidence are linked from `docs/project-memory.md` and the audit documents. The next code/doc wave must run the semantic update before claiming completion.
- Remaining risks: semantic runner availability, inferred-edge review, ordinary Liability CRUD, Balance query/valuation/currency semantics, external infrastructure, populated restore, browser E2E, staging/release observation and legacy adapter closure remain open.
