# P1-G-04 Evidence

- Task ID: P1-G-04
- Evidence status: PASS-E2E
- Baseline commit: `081084a`
- Verified branch/commit: `codex/p1-g04-e2e-closure@e9a14bbe7401be6a9d0e7ea39ccd53fbb0a9ea7f`
- Green run: [GitHub Actions 34075512188](https://github.com/QZSAMA/HomeFinance/actions/runs/34075512188)
- Scope: Playwright Chromium coverage against the isolated six-service Compose stack and deterministic server-side AI mock. The closure fixes changed test topology plus one frontend transaction-form state defect; no backend route, authorization, financial, or persistence contract changed.

## RED and failure-to-green record

The original baseline had no Playwright dependency, configuration, browser test, or E2E script:

```text
frontend> npm run test:e2e
npm error Missing script: "test:e2e"
```

Real GitHub-hosted runs then exposed the stack and browser contracts progressively:

| Run | Observed failure | Resolution |
| --- | --- | --- |
| `33740088163` | PostgreSQL, Redis, MinIO, mock-AI, and backend became healthy; frontend health check failed on `localhost` before Playwright | Probe `127.0.0.1` and retain failure diagnostics until explicit cleanup |
| `33741826576` | The first journey matched `/check-duplicate` 200 instead of ledger creation 201 | Match the complete resource pathname suffix |
| `33841275613` | The first journey passed; CRUD journey exposed stale `editingId`, causing a new expense to call `PUT /expenses/<incomeId>` | `7b8a02f` clears edit state on successful save, cancel, and new-record open; focused `TransactionsPage` regression added |
| `34074052760` | First two journeys passed; the viewer journey's family-name locator resolved both an `<option>` and `<h3>` | `dd60c22` targets the accessible family heading |
| `34074632143` | First two journeys passed; after invite success, dynamic `.last()` retargeted from the unmounted invite modal to the still-visible family modal | `e9a14bb` anchors the invite dialog to its accessible heading; failure artifact upload and final teardown both passed |
| `34075512188` | All four journeys passed | Gate closed as PASS-E2E |

No test was skipped, weakened, or changed to accept a neighboring response. No arbitrary sleep was added.

## Green Actions evidence

- Runner: GitHub-hosted `ubuntu-latest`, resolved to Ubuntu 24.04.4 / image `ubuntu-24.04@20260831.293.1`.
- Job: `Playwright against Compose`, successful in 2m14s; run completed in 2m19s from `2026-09-07T02:12:23Z` to `02:14:42Z`.
- Services: PostgreSQL, Redis, MinIO, mock-AI, backend, and Nginx frontend all reported healthy before browser execution.
- Browser execution: Chromium, one worker, `4 passed (8.2s)`:
  1. admin registration, family switching, and tenant data isolation — 1.5s;
  2. admin CRUD reconciles in the income statement — 1.3s;
  3. viewer mutation is denied and leaves the ledger unchanged — 939ms;
  4. CSV confirmation and deterministic AI proposal require confirmation before mutation — 1.6s.
- Cleanup: the final `docker compose ... down --volumes --remove-orphans` step succeeded; all six containers, disposable PostgreSQL/MinIO volumes, and the Compose network were removed.
- Success artifacts: failure-only diagnostic upload was correctly skipped. Run `34074632143` separately proves screenshot/trace/report upload and teardown on the failure path.

## Implemented coverage

- `frontend/playwright.config.ts` runs one serial Chromium worker against `http://127.0.0.1:4173` and retains failure artifacts.
- `frontend/tests/phase1-critical-journeys.spec.ts` covers registration/family switching and tenant isolation; income/expense CRUD with income-statement reconciliation; a real viewer `403` denial with no resulting ledger row; and CSV batch plus AI proposal confirmation boundaries.
- `docker-compose.e2e.yml` starts PostgreSQL, Redis, MinIO, deterministic server-side `mock-ai`, backend, and Nginx frontend. The frontend is exposed only on `127.0.0.1:4173`.
- The backend container applies existing Prisma migrations before startup. No production secret or external AI provider is used.

## Final regression evidence

| Command | Result |
| --- | --- |
| `frontend> npx playwright test --list` | PASS — 4 Chromium journeys discovered |
| `frontend> npm test` | PASS — 16 files / 37 tests |
| `frontend> npm run lint` | PASS — no warning lines |
| `frontend> npm run build` | PASS — existing 870.27 kB minified / 241.20 kB gzip main-chunk warning remains |
| `backend> npm run build` | PASS |
| `backend> npm test -- --runInBand --coverage` | PASS — 55 suites / 458 tests; statements 77.73%, branches 61.95%, functions 73.28%, lines 79.14% |
| `backend> npx --no-install prisma validate` | PASS — Prisma 5.22.0 |
| `backend> npx --no-install prisma format --check` | EXISTING GATE FAILURE — reports unformatted schema; this E2E branch has no schema diff and did not reformat unrelated persistence files |
| `frontend> npm run test:e2e` locally | ENVIRONMENT BLOCKED — local Docker CLI is unavailable; the authoritative Docker-capable Actions run passed |

Existing backend negative-path fixtures still emit their expected AI/Redis/MinIO console diagnostics. The Actions run also reports the upstream Node.js 20 action-runtime deprecation warning; neither warning changed the asserted result.

## Security, infrastructure, and remaining risk

- The viewer journey submits a real expense mutation, observes backend `403`, and verifies through the administrator context that no denied marker exists.
- Tenant switching hides the other family's ledger marker, and the report journey reconciles the created income and expense.
- AI output remains proposal-only until explicit confirmation; CSV import uses the server-owned confirmation path.
- P1-G-04 proves the normal six-service Compose/browser path only. It does not close P1-G-05 Redis/MinIO outage/recovery, object isolation, production-like populated restore, staging/release observation, P1-H-03, or P1-H-04.
- Graphify semantic refresh remains pending because the installed runner is AST-only and would replace the reviewed semantic graph; no misleading graph refresh was performed.
