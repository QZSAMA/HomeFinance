# P1-G-04 Evidence

- Task ID: P1-G-04
- Evidence status: IMPLEMENTED / E2E_EXECUTION_BLOCKED
- Baseline commit: 081084a
- Branch/commit: `codex/phase1-ledger-trust@9e89525`
- Scope: Playwright Chromium coverage, an isolated Compose stack, a deterministic server-side AI mock, and container-startup corrections needed by the stack. No business route behavior was changed.

## RED

```text
frontend> npm run test:e2e
npm error Missing script: "test:e2e"
```

The baseline had no Playwright dependency, configuration, browser test, or E2E script.

## Implemented coverage

- `frontend/playwright.config.ts` runs one serial Chromium worker against `http://127.0.0.1:4173` and retains failure artifacts.
- `frontend/tests/phase1-critical-journeys.spec.ts` defines four isolated-user journeys: registration/family switching and tenant isolation; income/expense CRUD with income-statement reconciliation; a viewer `403` mutation denial with no visible ledger write; CSV batch confirmation plus persisted, user-confirmed AI proposal execution.
- `docker-compose.e2e.yml` starts PostgreSQL, Redis, MinIO, server-side `mock-ai`, backend, and Nginx frontend. The frontend is exposed only on `127.0.0.1:4173`.
- `e2e/mock-ai` returns one deterministic `create_expense` proposal, so the AI confirmation journey does not call an external provider.
- The backend image now runs `prisma migrate deploy` before `dist/server.js`; Prisma is a production dependency so that migration command is available in the production image.

## Regression evidence

| Command | Result |
| --- | --- |
| `frontend> npm test` | PASS — 7 files, 19 tests |
| `frontend> npm run lint` | PASS — 16 existing warnings remain |
| `frontend> npm run build` | PASS — existing 859.52 kB main-bundle warning remains |
| `backend> npm run build` | PASS |
| `backend> npx prisma validate` | PASS |
| `backend> npx prisma format --check` | PASS |
| `frontend> npx playwright test --list` | PASS — 4 Chromium journeys discovered |
| `python` YAML parse of `docker-compose.e2e.yml` | PASS — six services parsed |
| `frontend> npm run test:e2e` | BLOCKED — `Docker is not available. Start Docker Desktop and ensure the docker CLI is on PATH.` |

The lint and bundle messages predate this task and were not weakened or suppressed.

## GitHub Actions handoff (2026-09-03)

- Added `.github/workflows/e2e.yml`, an independent workflow for `main` pushes,
  pull requests, and manual dispatch on `ubuntu-latest`.
- The workflow installs Node.js 20 and Chromium with Linux dependencies, then
  invokes the existing `frontend> npm run test:e2e` entry point. Compose
  lifecycle remains owned by `frontend/scripts/run-e2e.mjs`.
- Failed runs upload `frontend/playwright-report` and `frontend/test-results`
  for 14 days. The workflow has `contents: read` permissions and no production
  secrets.
- Local YAML parsing, frontend 13-file/31-test regression, lint, build,
  backend build, and backend 55-suite/458-test regression pass. The workflow
  itself is **CI_READY** only; P1-G-04 remains **E2E_EXECUTION_PENDING** until
  a real GitHub runner produces a green run.

## Security, database, and release status

- The viewer-denial journey submits an expense mutation and waits for the real backend `403`; the administrator context then verifies the denied marker is absent. It is designed but not browser-executed because the Compose stack cannot start.
- The E2E stack would apply the existing Prisma migrations to its named disposable PostgreSQL volume. No migration or real Redis/MinIO/backend interaction was executed on this machine.
- Release/staging evidence: not run. This local test stack is not release evidence.

## Required environment to close the block

1. Start Docker Desktop and make the `docker` CLI available on `PATH`.
2. From `frontend/`, install the local browser if it is not already present: `npx playwright install chromium`.
3. Validate Compose semantics: `docker compose -p homefinance-e2e -f docker-compose.e2e.yml config --quiet`.
4. Run `npm run test:e2e`; on failure, rerun with `E2E_KEEP_STACK=1` to inspect the stack and Playwright artifacts, then explicitly clean it with the documented Compose `down --volumes --remove-orphans` command.

## Graphify and remaining risks

- This task adds a local test deployment topology. The installed Graphify runner is known to produce an AST-only graph that would replace the reviewed semantic graph, so no misleading graph refresh was performed; semantic Graphify refresh remains pending until its intended runner is available.
- P1-G-04 must not be marked PASS-E2E until the four journeys run against the actual Compose services on a Docker-capable machine.
- The GitHub-hosted Ubuntu runner is now the preferred Docker-capable execution
  target; a green Actions run is required before changing this evidence to
  PASS-E2E.
- The first GitHub Actions attempt (run `33740088163`) built all images and
  reached healthy PostgreSQL, Redis, MinIO, mock-AI, and backend services, but
  the frontend container failed its `localhost` health check before Playwright
  started. The E2E health check now probes `127.0.0.1`, and the workflow keeps
  the stack long enough to dump Compose diagnostics before cleanup.
