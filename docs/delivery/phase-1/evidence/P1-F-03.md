# P1-F-03 Evidence

- Task ID: P1-F-03
- Evidence status: REGRESSION_VERIFIED
- Baseline commit: 081084a
- Branch/commit: codex/p1-f-timezone; pending implementation commit
- Source evidence: `docs/superpowers/specs/2026-09-03-family-timezone-period-currency-design.md` §7; implementation plan Task 6
- Affected families/roles/routes/tables: authenticated family members; `GET /api/families/:familyId/budgets/progress`, budget CRUD, `GET /api/compare/summary`, `Budget.currency`
- RED command: `cd backend; $env:DATABASE_URL='postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public'; $env:RUN_INTEGRATION='1'; npm test -- --runInBand src/tests/budgetCompare.periodCurrency.integration.test.ts`
- RED failure summary: Before implementation, budget progress returned `spent=0` for the family-local boundary fixture and compare omitted `conversionStatus`/nullable totals.
- GREEN commit and scope: Pending commit; adds additive `Budget.currency`, shared PeriodWindow/currency semantics to budget progress, centralized family access on budget reads, and month-scoped compare responses.
- REFACTOR summary: Uses half-open `[gte, lt)` windows, one captured reference instant per progress request, Decimal-safe currency summaries, and stable DomainError handling.
- Regression commands/results: `npm test -- --runInBand src/routes/budgets.test.ts src/routes/compare.test.ts` → 17/17; `npm run build` → PASS; `npm run test:integration` → 16 suites / 92 tests PASS.
- Security negative matrix: budget list/progress now authorize through `requireFamilyAccess` before family settings or expense access; existing viewer/non-member role matrix remains green.
- Database/migration evidence: additive migration `20260903100100_add_budget_currency` applied on PostgreSQL 18.1 at `localhost:5433`; `npx prisma validate`, `npx prisma format --check`, `npx prisma migrate deploy` PASS.
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: Local PostgreSQL and focused route tests pass; Docker/Redis/MinIO/staging remain unobserved.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: To be recorded with the implementation commit; semantic Graphify refresh remains pending because only AST-only tooling is available.
- Remaining risks: Frontend budget/compare contracts, GoalContribution and Balance valuation remain open; release and external infrastructure gates remain open.
