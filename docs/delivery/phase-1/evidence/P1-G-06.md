# P1-G-06 Evidence

- Task ID: P1-G-06
- Evidence status: FAILED
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; this P1-G-06 quality slice hardens the Prisma financial-mutation adapter JSON boundary and extends its contract tests. The current slice makes the MinIO startup probe propagate failure to its lifecycle owner and proves server degradation/shutdown behavior.
- Source evidence: See phase-1-tracker.md and the Phase 1 design specification
- Affected families/roles/routes/tables: family-scoped `FinancialMutationStore` root/transaction adapter; IdempotencyRecord, Income, Expense and AuditEvent JSON boundaries; process-startup MinIO availability boundary in `config/minio.ts` → `server.ts`; no family-scoped route/API or financial-data behavior changes
- RED command: `cd backend; npm test -- --runInBand src/config/minio.test.ts`
- RED failure summary: Historical adapter REDs are retained below. The current RED command `npm test -- --runInBand src/config/minio.test.ts` failed exactly because a rejected `bucketExists()` call was caught and converted to a resolved `undefined`, preventing the lifecycle owner from observing the failure.
- GREEN commit and scope: The current working tree preserves the existing adapter contract tests and adds parameterized `NaN`, `Infinity`, and `-Infinity` cases. `toJson()` now accepts only finite JavaScript numbers and rejects non-finite JSON before Prisma receives an IdempotencyRecord, AuditEvent, or other adapter write. The follow-up JSON-boundary slice adds parameterized `Map`, `Set`, and `RegExp` cases; `toJson()` now rejects non-plain objects instead of silently persisting them as `{}`. The current slice removes `ensureBucket()`'s local catch so its owner, `startServer()`, controls the documented degraded-start policy; `server.test.ts` proves MinIO failure does not stop the listener and repeated shutdown closes/disconnects exactly once. No coverage threshold was weakened.
- REFACTOR summary: The initial fixture incorrectly treated `undefined` within a JSON array as valid; the adapter correctly rejected it, so the assertion was moved to the rejection contract rather than widening JSON serialization behavior. The lifecycle test mocks only listener, database, Redis and MinIO network boundaries, invokes the real exported server startup behavior, and removes its signal listener after asserting shutdown to prevent test-process leakage.
- Regression commands/results: Historical REDs: `npm test -- --runInBand src/services/prismaFinancialMutationStore.test.ts` failed exactly because all three non-finite numbers were accepted; the follow-up RED failed because `Map`, `Set`, and `RegExp` were resolved as `{}`. Current GREEN: `npm test -- --runInBand src/config/minio.test.ts src/server.test.ts` passes 2 suites / 5 tests; `npm run build` passes. The required coverage command executes 34 suites / 281 tests; statements 63.22%, branches 46.01%, functions 64.06%, lines 63.58%. Branch coverage improved from 45.38% but remains below 60%, so the quality gate remains failed without changing the threshold. Existing OCR/MinIO fixtures emit known fixture console warnings.
- Security negative matrix: Tests assert malformed JSON, invalid Date, function values, undefined array entries, all non-finite numeric values, and non-plain objects (`Map`, `Set`, `RegExp`) fail before IdempotencyRecord persistence; family/actor/operation scope is retained through snapshot mapping. The lifecycle negative test asserts a MinIO initialization failure is observable by `server.ts` without preventing controlled startup. Route-level unauthenticated/non-member/viewer coverage remains pending route adoption.
- Database/migration evidence: NOT_RUN unless task is design-only
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: The gate remains FAILED, but the Prisma adapter now rejects JSON numeric values that JSON/Prisma cannot represent reliably and non-plain objects that would otherwise collapse to `{}` instead of allowing invalid or lossy financial/audit payloads to reach persistence. MinIO initialization failure now reaches the lifecycle owner, which logs the error while preserving startup and idempotent cleanup instead of burying the availability signal in the storage helper. The remaining global branch deficit requires further behavior-focused tests. Lowering the threshold is not acceptable.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: Tracker and project memory reflect the new measured baseline. Graphify updates for this mixed code/document slice must semantically re-extract the changed sources, replace their owned nodes and edges by source provenance, and review the resulting diff before generated artifacts are committed. AST-only `graphify update .` is not acceptable because it can discard existing source-backed semantic edges.
- Remaining risks: This card does not close the gate. Untested route, AI, infrastructure and legacy-resource branches dominate the deficit; any new coverage work must retain the 60% threshold and keep assertion quality.
