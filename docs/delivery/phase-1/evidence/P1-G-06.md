# P1-G-06 Evidence

- Task ID: P1-G-06
- Evidence status: FAILED
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; this P1-G-06 quality slice hardens the Prisma financial-mutation adapter JSON boundary and extends its contract tests
- Source evidence: See phase-1-tracker.md and the Phase 1 design specification
- Affected families/roles/routes/tables: family-scoped `FinancialMutationStore` root/transaction adapter; IdempotencyRecord, Income, Expense and AuditEvent JSON boundaries; no route/API behavior changes
- RED command: `backend/npm test -- --runInBand --coverage`
- RED failure summary: On 2026-08-31 all 32 suites / 269 tests passed, but the required global gate failed at statements 60.49%, branches 44.00%, functions 57.50%, lines 61.21%; branch/function thresholds are 60%.
- GREEN commit and scope: The current working tree preserves the existing adapter contract tests and adds parameterized `NaN`, `Infinity`, and `-Infinity` cases. `toJson()` now accepts only finite JavaScript numbers and rejects non-finite JSON before Prisma receives an IdempotencyRecord, AuditEvent, or other adapter write. The follow-up JSON-boundary slice adds parameterized `Map`, `Set`, and `RegExp` cases; `toJson()` now rejects non-plain objects instead of silently persisting them as `{}`. No coverage threshold was weakened.
- REFACTOR summary: The initial fixture incorrectly treated `undefined` within a JSON array as valid; the adapter correctly rejected it, so the assertion was moved to the rejection contract rather than widening JSON serialization behavior.
- Regression commands/results: RED: `npm test -- --runInBand src/services/prismaFinancialMutationStore.test.ts` failed exactly because all three non-finite numbers were accepted; the follow-up RED failed because `Map`, `Set`, and `RegExp` were resolved as `{}`. GREEN: the same command passes 1 suite / 10 tests. `npm run build` passes. The required coverage command executes 33 suites / 279 tests; statements 62.21%, branches 45.38%, functions 62.18%, lines 62.63%. Functions clear the 60% threshold, but branch coverage remains below 60%, so the quality gate remains failed without changing the threshold. Existing OCR/MinIO fixtures emit known fixture console warnings.
- Security negative matrix: Tests assert malformed JSON, invalid Date, function values, undefined array entries, all non-finite numeric values, and non-plain objects (`Map`, `Set`, `RegExp`) fail before IdempotencyRecord persistence; family/actor/operation scope is retained through snapshot mapping. Route-level unauthenticated/non-member/viewer coverage remains pending route adoption.
- Database/migration evidence: NOT_RUN unless task is design-only
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: The gate remains FAILED, but the Prisma adapter now rejects JSON numeric values that JSON/Prisma cannot represent reliably and non-plain objects that would otherwise collapse to `{}` instead of allowing invalid or lossy financial/audit payloads to reach persistence. The adapter-focused slices preserve ordinary objects, arrays, dates, nulls, and finite numbers while improving its local contract coverage. The remaining global branch deficit requires further behavior-focused tests. Lowering the threshold is not acceptable.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: Tracker and project memory reflect the new measured baseline. The scoped semantic Graphify refresh is complete: 655 nodes and 643 edges; it replaces 11 stale TypeScript structural nodes and adds 8 reviewed, source-backed semantic relationships, all `EXTRACTED` with confidence 1.0. Generated `graph.json`, `graph.html`, `GRAPH_REPORT.md`, `manifest.json`, and `cost.json` are synchronized.
- Remaining risks: This card does not close the gate. Untested route, AI, infrastructure and legacy-resource branches dominate the deficit; any new coverage work must retain the 60% threshold and keep assertion quality.
