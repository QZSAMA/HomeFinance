# P1-G-06 Evidence

- Task ID: P1-G-06
- Evidence status: FAILED
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; P1-G-06 quality slice in the current worktree adds the Prisma financial-mutation adapter contract tests
- Source evidence: See phase-1-tracker.md and the Phase 1 design specification
- Affected families/roles/routes/tables: family-scoped `FinancialMutationStore` root/transaction adapter; IdempotencyRecord, Income, Expense and AuditEvent JSON boundaries; no route/API behavior changes
- RED command: `backend/npm test -- --runInBand --coverage`
- RED failure summary: On 2026-08-31 all 32 suites / 269 tests passed, but the required global gate failed at statements 60.49%, branches 44.00%, functions 57.50%, lines 61.21%; branch/function thresholds are 60%.
- GREEN commit and scope: Current working tree adds four `prismaFinancialMutationStore` contract tests for replay JSON Date/null preservation, audit JSON database-null semantics, Decimal conversion, invalid/non-JSON rejection before persistence, and root snapshot operation validation. No production behavior or coverage threshold was weakened.
- REFACTOR summary: The initial fixture incorrectly treated `undefined` within a JSON array as valid; the adapter correctly rejected it, so the assertion was moved to the rejection contract rather than widening JSON serialization behavior.
- Regression commands/results: Focused adapter test: 1 suite / 4 tests PASS. `npm run build` PASS. The required coverage command now executes 33 suites / 273 tests; statements 62.14%, branches 45.18%, functions 62.18%, lines 62.58%. Functions now clear the 60% threshold, but branch coverage remains below 60%, so Jest exits nonzero only for the global branch threshold. Existing OCR/MinIO fixtures emit console warnings.
- Security negative matrix: Tests assert malformed JSON fails before IdempotencyRecord persistence; family/actor/operation scope is retained through snapshot mapping. Route-level unauthenticated/non-member/viewer coverage remains pending route adoption.
- Database/migration evidence: NOT_RUN unless task is design-only
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: N/A
- Observed result: The gate remains FAILED, but its initial Prisma adapter slice eliminated the global function deficit and added regression coverage at a financial transaction boundary. The remaining branch deficit requires further behavior-focused tests; lowering the threshold is not acceptable.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: Tracker and project memory reflect the new measured baseline. The scoped semantic Graphify refresh is complete: 631 nodes and 617 edges; five test/evidence nodes were added or refreshed, and the four new relationships are reviewed, source-backed `EXTRACTED` edges.
- Remaining risks: This card does not close the gate. Untested route, AI, infrastructure and legacy-resource branches dominate the deficit; any new coverage work must retain the 60% threshold and keep assertion quality.
