# P1-G-06 Evidence

- Task ID: P1-G-06
- Evidence status: FAILED
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust@bdabe7b plus the current evidence update; earlier P1-G-06 slices harden the Prisma financial-mutation adapter JSON boundary and MinIO lifecycle boundary, while the latest measurement includes AI proposal confirmation/history, AI Balance confirmation and the Playwright/Compose foundation
- Source evidence: See phase-1-tracker.md and the Phase 1 design specification
- Affected families/roles/routes/tables: family-scoped `FinancialMutationStore` root/transaction adapter; IdempotencyRecord, Income, Expense and AuditEvent JSON boundaries; process-startup MinIO availability boundary in `config/minio.ts` → `server.ts`; no family-scoped route/API or financial-data behavior changes
- RED command: `cd backend; npm test -- --runInBand src/config/minio.test.ts`
- RED failure summary: Historical adapter REDs are retained below. The current RED command `npm test -- --runInBand src/config/minio.test.ts` failed exactly because a rejected `bucketExists()` call was caught and converted to a resolved `undefined`, preventing the lifecycle owner from observing the failure.
- GREEN commit and scope: The earlier working tree preserves the adapter and lifecycle contract tests: `toJson()` rejects non-finite JSON and non-plain objects before Prisma persistence, while `ensureBucket()` propagates startup failure to `startServer()` and server lifecycle tests prove controlled degradation and idempotent shutdown. The current test-only slice adds 14 regression cases: malformed canonical payloads never open a coordinator transaction; mismatched membership never reaches idempotency lookup; corrupt stored replay results fail closed; unrelated Prisma conflicts do not read an idempotency winner; and invalid ledger commands are rejected before authorization. No coverage threshold or production behavior was weakened or changed.
- REFACTOR summary: The initial fixture incorrectly treated `undefined` within a JSON array as valid; the adapter correctly rejected it, so the assertion was moved to the rejection contract rather than widening JSON serialization behavior. The lifecycle test mocks only listener, database, Redis and MinIO network boundaries, invokes the real exported server startup behavior, and removes its signal listener after asserting shutdown to prevent test-process leakage.
- Regression commands/results: Historical adapter/coordinator RED/GREEN evidence remains valid. The latest required `npm test -- --runInBand --coverage` run executes 50 suites / 411 tests with every behavior assertion passing; statements are 75.61%, branches 60.14%, functions 71.42%, and lines 76.40%. The unchanged 60% global threshold passes. `npm run build`, Prisma validate/format, AI/Balance focused regression and real PostgreSQL integration (11 suites / 76 tests) pass. Existing OCR/MinIO fixtures emit known warnings; Redis/MinIO were unavailable for the new AI route integration.
- Security negative matrix: Tests assert malformed JSON, invalid Date, function values, undefined array entries, all non-finite numeric values, and non-plain objects (`Map`, `Set`, `RegExp`) fail before IdempotencyRecord persistence; family/actor/operation scope is retained through snapshot mapping. The new coordinator tests show malformed canonical inputs never open a transaction, family/user mismatches fail before idempotency lookup, and corrupt replay records fail closed. P1-E-03 adds allow-listed AI create-action normalization, current-membership authorization, forged-role rejection, malformed/query/source mismatch rejection, and real HTTP viewer/non-member zero-side-effect evidence. Ledger command validation rejects invalid amount/category/currency/source before membership lookup. The legacy raw `/execute-actions` compatibility path, AI confirmation tamper/replay/concurrency and external object isolation remain open.
- Database/migration evidence: P1-E-02 records PASS-REAL existing/fresh PostgreSQL migration and AI proposal constraints; P1-E-03 records PASS-REAL text/OCR route persistence and zero-ledger evidence. These do not close the global coverage gate.
- Frontend/E2E evidence: frontend tests/lint/build and Playwright discovery are PASS; real browser E2E remains BLOCKED by unavailable Docker and does not count toward this gate
- Release environment/time: N/A
- Observed result: The coverage gate is PASS-REAL. Adapter/lifecycle/coordinator, Import/Recurring, text-AI pre-confirmation safety, AI proposal schema, chat/OCR persistence and AI Balance confirmation are covered across 50 / 411 tests, with branch coverage at 60.14%. The composite quality review remains IN_REVIEW/AT_RISK because dependency advisories, existing frontend lint warnings, oversized frontend output, external-service verification, browser E2E and release observation remain open.
- Rollback rehearsal: Must be recorded before REGRESSION_VERIFIED; additive migration and forward-fix policy applies
- ADR/project-memory/audit/Graphify updates: tracker, project memory, ADR-0007 and audit notes reflect the latest 411-test/60.14% branch measurement, AI Balance confirmation and Docker-blocked E2E. The installed Graphify updater remains AST-only and would replace the reviewed semantic graph, so no degraded graph artifact is accepted; semantic refresh remains pending.
- Remaining risks: This card closes the backend coverage gate but not the composite Phase 1 quality gate. Untested external-service, browser, release and legacy-resource paths remain; future coverage work must retain the 60% threshold and keep assertion quality.
