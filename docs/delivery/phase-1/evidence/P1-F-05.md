# P1-F-05 三表对账与报表语义门禁

- Task ID：P1-F-05
- Lifecycle：`BACKLOG`（基础工具和报表 fixture 已开始，尚未覆盖所有消费方）
- Evidence status：`PASS-REAL`（reports period/currency fixture）+ `PASS-MOCK`（reconciliation helpers）
- Health：`AT_RISK`
- Baseline commit：`081084a`
- Current implementation：`7af352b`（currency/reconciliation helpers）、`78ece16`（report route adoption）
- Source evidence：`docs/superpowers/specs/2026-09-03-family-timezone-period-currency-design.md` §8、§10
- Affected scope：income-statement、cash-flow、balance-sheet、summary；Family timezone/baseCurrency 配置
- RED：新报表合同在迁移前缺少 `window`、`totalsByCurrency` 和混币种安全行为；focused route tests观察到旧实现使用服务器日期和直接 reduce。
- GREEN：`78ece16` 统一 family-local `[gte, lt)` 查询、币种摘要、分类分组和 `reconciliationStatus`；`reports.periodCurrency.integration.test.ts` 在 PostgreSQL 18.1 验证本地日界、混币种余额和四类现金流 3/3。
- Regression：后端 coverage 54 suites / 455 tests（statements 77.87%、branches 62.40%、functions 73.29%、lines 78.87%）通过；报表 focused 15/15。
- Security/authorization：所有 report routes 保持 `authMiddleware → requireFamilyAccess → cacheMiddleware`，家庭配置读取发生在授权之后；viewer/non-member 负向路径由既有 role matrix 覆盖。
- Database/migration：使用已应用的 13 migrations；本卡未新增 schema。
- Frontend/E2E/release：前端尚未适配 `null`/grouped totals 展示；Docker/Playwright、Redis/MinIO、staging/release observation 仍未运行。
- Rollback：保留旧字段名；新窗口/币种字段可前向兼容回退，禁止将 `null` 改回伪造 0。
- ADR/project-memory/audit/Graphify：ADR-0005、project-memory、审计进度已更新；semantic Graphify refresh 仍待可用 runner。
- Remaining risks：Budget、Compare、GoalContribution、Balance valuation/as-of 和前端显示仍未接入；未满足 Phase 1 退出门禁。
