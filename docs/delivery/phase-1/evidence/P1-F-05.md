# P1-F-05 三表对账与报表语义门禁

- Task ID：P1-F-05
- Lifecycle：`REGRESSION_VERIFIED`
- Evidence status：`PASS-REAL`（PostgreSQL reports/dashboard fixture）+ `PASS-MOCK`（reconciliation helpers）
- Health：`AT_RISK`
- Baseline commit：`081084a`
- Current implementation：`7af352b`（currency/reconciliation helpers）、`78ece16`（report route adoption）、`d8ec3ad`（frontend safe rendering）、`9134609`（dashboard/compare reconciliation contract）
- Source evidence：`docs/superpowers/specs/2026-09-03-family-timezone-period-currency-design.md` §8、§10
- Affected scope：income-statement、cash-flow、balance-sheet、summary；Family timezone/baseCurrency 配置
- RED：新增 fixture 先验证出三处缺口：Dashboard 缺少 `valuationRuleVersion`、资产精确但收入混币种时错误返回 `conversionStatus=exact`、混币种投资配置仍返回原币种伪合计。
- GREEN：`9134609` 让 Dashboard 汇总传播全部收入/支出/资产/负债的币种状态，统一返回 `valuationAsOf`/`valuationRuleVersion`，并在混币种资产时将投资配置值置为 `null`；Compare 同步返回估值版本和对账状态。`reports.periodCurrency.integration.test.ts` 在 PostgreSQL 18.1 验证窗口边界、混币种安全、四类现金流、Dashboard/独立报表/Compare 恒等式 6/6。
- Regression：后端 coverage 55 suites / 458 tests（statements 77.73%、branches 61.95%、functions 73.28%、lines 79.14%）通过；PostgreSQL integration 17 suites / 97 tests；前端 13 files / 31 tests、lint、build 通过。
- Security/authorization：所有 report routes 保持 `authMiddleware → requireFamilyAccess → cacheMiddleware`，家庭配置读取发生在授权之后；viewer/non-member 负向路径由既有 role matrix 覆盖。
- Database/migration：使用已应用的 13 migrations；本卡未新增 schema。
- Frontend/E2E/release：`d8ec3ad` 已让 Dashboard、三表、预算、compare、目标和投资页适配 `null`/grouped totals、窗口/时区标签及勾稽提示；前端 13 个测试文件/31 个测试、lint/build PASS。Docker/Playwright、Redis/MinIO、staging/release observation 仍未运行。
- Rollback：保留旧字段名；新窗口/币种字段可前向兼容回退，禁止将 `null` 改回伪造 0。
- ADR/project-memory/audit/Graphify：ADR-0005、project-memory、审计进度已更新；semantic Graphify refresh 仍待可用 runner。
- Remaining risks：当前估值仍是 `current-snapshot-v1` 快照，未提供历史估值/FX 审计模型；Docker/Playwright、Redis/MinIO、staging/release observation 和 Phase 1 退出门禁仍开放。
