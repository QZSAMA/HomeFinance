# P1-D-02 Evidence

- Task ID: P1-D-02
- Evidence status: PASS-REAL for local PostgreSQL atomic recurring execution
- Baseline commit: 582db55 on codex/phase1-ledger-trust
- Branch/commit: codex/phase1-ledger-trust@370b2d9
- Source evidence: backend/src/services/recurringService.ts, backend/src/services/prismaRecurringExecutionStore.ts, backend/src/services/recurringService.phase1.test.ts, and backend/src/tests/recurring.phase1.integration.test.ts
- Affected families/roles/routes/tables: authenticated family admin/member; RecurringTransaction, RecurringExecution, Income/Expense, IdempotencyRecord, AuditEvent, and trigger-driven Family.cacheVersion participate in the same PostgreSQL transaction
- RED command: npm test -- --runInBand src/services/recurringService.phase1.test.ts; the initial suite failed because recurring execution did not expose an application service and the legacy route performed separate entry creation and rule update
- RED failure summary: inactive/future/end-date guards, occurrence arbitration, transaction-backed Ledger invocation, conditional rule advance, replay result, and rollback behavior were absent
- GREEN scope: executeRecurring authorizes through the coordinator, loads a non-tombstoned family-scoped rule, resolves explicit scheduledFor, validates due/active/end-date state, creates the occurrence execution, invokes the transaction-backed Income/Expense Ledger command, conditionally advances nextDate/version, stores the committed result, and appends audit/idempotency facts in one transaction
- Atomicity scope: a rule-advance failure aborts the Income/Expense row, RecurringExecution, idempotency result, and audit. The real failure-injection test observes unchanged ledger/execution counts after the forced failure
- Stable errors: inactive rules return RULE_INACTIVE; stale, future, and after-end occurrences return RECURRING_NOT_DUE; conditional rule races map to retryable concurrency behavior without partial facts
- REFACTOR summary: occurrence replay serialization is validated before returning; the coordinator's operation resource is the RecurringExecution, while the generated ledger fact is separately exposed as entryId. Ledger effective date is the scheduled occurrence, not retry time
- Regression commands/results: service focused suite PASS, 1 suite / 6 tests; backend build PASS; full integration PASS, 8 suites / 47 tests; full backend coverage executes 42 suites / 344 tests with all assertions passing and fails only the unchanged 60% branch threshold at 47.68%
- Security negative matrix: service rejects invalid identifiers/key before mutation; family membership and writer role are checked before execution or replay lookup; inactive/future/end-date cases produce zero ledger/execution facts; concurrency and rollback are PASS-REAL. Malformed HTTP payload remains handled by the route Zod/stable-error boundary
- Database/migration evidence: PostgreSQL 18.1 PASS-REAL; the recurring integration suite has 6 cases, including three table-driven invalid-state cases and forced transaction rollback
- Frontend/E2E evidence: frontend occurrence/key propagation is separately recorded in P1-D-03/P1-G-03; browser E2E NOT_RUN
- Release environment/time: local Windows/PostgreSQL 18.1, 2026-09-01; staging/release observation NOT_RUN
- Observed result: REGRESSION_VERIFIED / AT_RISK; the transaction contract is proven locally, but external infrastructure, populated migration/restore, and release observation remain open
- Rollback rehearsal: forced transaction rollback PASS-REAL; release rollback NOT_RUN. Disable the execute adapter or restore the previous code while preserving additive execution/audit rows; never fall back to separate entry/rule commits
- ADR/project-memory/audit/Graphify updates: ADR-0002 remains the general database-arbitration decision and ADR-0006 specializes recurring occurrence semantics; tracker, memory, and audit updated. Semantic Graphify refresh remains pending
- Remaining risks: no automatic scheduler/worker lease is introduced; callers must still submit the intended occurrence, and production retry/latency behavior needs E2E and release observation
