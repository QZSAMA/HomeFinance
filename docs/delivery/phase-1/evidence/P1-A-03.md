# P1-A-03 Evidence

- Task ID: P1-A-03
- Evidence status: PASS-MOCK + PASS-REAL
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; real rollback-evidence commit pending
- Source evidence: Phase 1 design §4.1/§4.2/§9; implementation plan Task 3; ADR-0001 and ADR-0002
- Affected families/roles/routes/tables: FinancialMutationCoordinator and the LedgerApplicationService transaction port for family-scoped Income/Expense creates. The service rechecks member/admin membership in the transaction; viewer/non-member HTTP behavior and route adoption remain outside this unit. The coordinator writes IdempotencyRecord, ledger fact, AuditEvent and replay result in its transaction boundary.
- RED command: the original behavior RED remains `npm test -- --runInBand src/services/ledgerTransactionOrchestration.test.ts`; the new real-PostgreSQL evidence test runs `RUN_INTEGRATION=1 npm test -- --runInBand src/tests/phase1-concurrency.integration.test.ts` with the local non-production database URL.
- RED failure summary: the original focused test resolved successfully with an empty `resourceId` and therefore permitted audit/replay state to be written for an invalid mutation outcome. This was the intended RED and exposed an inconsistent transaction-result contract. The new slice changes no production behavior: it upgrades that already-GREEN contract from mock-only to real PostgreSQL evidence, so it has no separate behavior RED to claim.
- GREEN commit and scope: adds coordinator result validation immediately after the ledger executor returns and before AuditEvent or idempotency response persistence. An empty or whitespace-only resource ID now returns `VALIDATION_FAILED` 400; throwing from the transaction callback leaves actual Prisma transactions to roll back their prior idempotency and ledger writes.
- REFACTOR summary: the result validator is deliberately typed only to the coordinator's `resourceId` requirement, preserving generic executor record types. No route, schema, cache trigger, Redis behavior or external service path changed.
- Regression commands/results: the focused real rollback command passes 1 suite / 5 tests. With `DATABASE_URL=postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public`, `npm run test:integration` passes 3 suites / 25 tests; `npx prisma validate`, `npx prisma format --check`, and `npm run build` pass. The required coverage command executes 33 suites / 279 tests: statements 62.21%, branches 45.38%, functions 62.18%, lines 62.63%. The unchanged global branch threshold remains below 60%, so the quality gate remains failed; existing OCR/MinIO fixtures emit known console warnings.
- Security negative matrix: PASS-MOCK for invalid executor output: the coordinator rejects before AuditEvent and idempotency replay-result persistence. Existing coordinator contracts verify membership before idempotency lookup/write and zero effects for viewer. Unauthenticated/non-member and route-level negative coverage remains pending P1-A-04/P1-A-05.
- Database/migration evidence: no schema change. The new local PostgreSQL failure-injection test creates an Income inside the real transaction, returns a whitespace-only `resourceId`, receives `VALIDATION_FAILED` 400, and confirms that the family-scoped Income, IdempotencyRecord, and AuditEvent counts all remain unchanged. This exact invalid-result rollback contract is PASS-REAL on PostgreSQL 18.1 at localhost:5433.
- Frontend/E2E evidence: no HTTP or user-visible behavior is connected yet. The required frontend quality commands were rechecked on 2026-08-31: `npm run lint` exits 0 with the existing 16 warnings, and `npm run build` passes while retaining the existing 856.15 kB minified main-chunk warning; neither warning is introduced by this backend-only change.
- Release environment/time: local Windows development worktree, 2026-08-31; not released or observed in staging.
- Observed result: the transaction orchestration will not publish audit or replayable response state when an executor fails to return a valid resource reference, and the real transaction rolls back the already-inserted ledger fact and idempotency record. This is a partial P1-A-03 service-boundary hardening, not route-level exactly-once proof.
- Rollback rehearsal: the real failure-injection test is the rollback rehearsal: it starts from no new family facts, creates the IdempotencyRecord and Income inside one transaction, then forces validation failure and proves no Income, IdempotencyRecord, or AuditEvent persists. No migration or production fact is introduced; the established additive-schema forward-fix policy still applies to later route adoption.
- ADR/project-memory/audit/Graphify updates: project memory and tracker are synchronized with this commit. The incremental semantic Graphify refresh merges six source-backed `EXTRACTED` edges: P1-A-03 service-boundary hardening implements the pre-audit `resourceId` guard; the guard produces `VALIDATION_FAILED` for a blank reference; the real PostgreSQL failure injection verifies both that failure and zero persistence; and the tracker reports the resulting PASS-REAL evidence. The matching AST increment carries the integration-test and `command()` nodes plus seven source-backed import/containment edges, normalized to the existing Prisma, crypto, ledger-service, coordinator, type and store nodes. No `INFERRED` or unverified relationship is merged. ADR-0001/0002 remain the governing accepted decisions; no new ADR is required because data ownership and transaction topology do not change.
- Remaining risks: P1-A-02 and P1-G-00 remain below their global coverage gate, so this task cannot advance to `REGRESSION_VERIFIED`; the coordinator is still not adopted by Income/Expense POST/PUT/DELETE routes. Update/delete commands, route error mapping, `If-Match`, and HTTP replay evidence remain P1-A-04/P1-A-06 follow-up work.
