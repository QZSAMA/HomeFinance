# P1-A-03 Evidence

- Task ID: P1-A-03
- Evidence status: PASS-MOCK
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; current P1-A-03 commit (pending)
- Source evidence: Phase 1 design §4.1/§4.2/§9; implementation plan Task 3; ADR-0001 and ADR-0002
- Affected families/roles/routes/tables: FinancialMutationCoordinator and the LedgerApplicationService transaction port for family-scoped Income/Expense creates. The service rechecks member/admin membership in the transaction; viewer/non-member HTTP behavior and route adoption remain outside this unit. The coordinator writes IdempotencyRecord, ledger fact, AuditEvent and replay result in its transaction boundary.
- RED command: from `D:\Repo\Qz_Private\HomeFinance\.worktrees\phase1-ledger-trust\backend`, `npm test -- --runInBand src/services/ledgerTransactionOrchestration.test.ts`
- RED failure summary: the new focused test resolved successfully with an empty `resourceId` and therefore permitted audit/replay state to be written for an invalid mutation outcome. This was the intended RED and exposed an inconsistent transaction-result contract.
- GREEN commit and scope: adds coordinator result validation immediately after the ledger executor returns and before AuditEvent or idempotency response persistence. An empty or whitespace-only resource ID now returns `VALIDATION_FAILED` 400; throwing from the transaction callback leaves actual Prisma transactions to roll back their prior idempotency and ledger writes.
- REFACTOR summary: the result validator is deliberately typed only to the coordinator's `resourceId` requirement, preserving generic executor record types. No route, schema, cache trigger, Redis behavior or external service path changed.
- Regression commands/results: focused Ledger/coordinator/orchestration suite PASS, 3 suites / 19 tests; `npm run build` PASS; default Jest PASS, 32 suites / 268 tests. With `DATABASE_URL=postgresql://postgres:postgres@localhost:5433/homefinance_phase1_test?schema=public`, `npm run test:integration` PASS, 3 suites / 24 tests; `npx prisma validate` and `npx prisma format --check` PASS. The required coverage command executes all 32 suites / 268 tests but fails the unchanged global thresholds: statements 60.46%, branches 43.94%, functions 57.50%, lines 61.17%; branch/function remain below 60%.
- Security negative matrix: PASS-MOCK for invalid executor output: the coordinator rejects before AuditEvent and idempotency replay-result persistence. Existing coordinator contracts verify membership before idempotency lookup/write and zero effects for viewer. Unauthenticated/non-member and route-level negative coverage remains pending P1-A-04/P1-A-05.
- Database/migration evidence: no schema change. The latest coordinator code is included in the local PostgreSQL integration regression (P1-B-04), but this exact invalid-result failure injection is unit-port evidence only and is not claimed PASS-REAL.
- Frontend/E2E evidence: no HTTP or user-visible behavior is connected yet. The required frontend quality commands were rechecked on 2026-08-31: `npm run lint` exits 0 with the existing 16 warnings, and `npm run build` passes while retaining the existing 856.15 kB minified main-chunk warning; neither warning is introduced by this backend-only change.
- Release environment/time: local Windows development worktree, 2026-08-31; not released or observed in staging.
- Observed result: the transaction orchestration will not publish audit or replayable response state when an executor fails to return a valid resource reference. This is a partial P1-A-03 service-boundary hardening, not route-level exactly-once proof.
- Rollback rehearsal: revert the current P1-A-03 commit to remove the pre-audit result guard; no migration or persisted production fact is introduced. The established additive-schema forward-fix policy still applies to later route adoption.
- ADR/project-memory/audit/Graphify updates: project memory and tracker are synchronized with this commit. A narrow semantic Graphify refresh retained the previous graph and added only three source-backed `EXTRACTED` edges: the P1-A-03 pre-audit result guard, the P1-B-04 unclassified `INTERNAL_ERROR` risk, and the required raw Prisma-error capture. The extraction worker did not return a fragment, so no inferred or unverified edges were merged. ADR-0001/0002 remain the governing accepted decisions; no new ADR is required because data ownership and transaction topology do not change.
- Remaining risks: P1-A-02 and P1-G-00 remain below their global coverage gate, so this task cannot advance to `REGRESSION_VERIFIED`; the coordinator is still not adopted by Income/Expense POST/PUT/DELETE routes. Real failure-injection rollback for this exact invalid executor result, update/delete commands, route error mapping, `If-Match` and HTTP replay evidence remain P1-A-04/P1-A-06 follow-up work.
