# P1-B-01 Evidence

- Task ID: P1-B-01
- Evidence status: PASS-REAL
- Baseline commit: 081084a
- Branch/commit: codex/phase1-ledger-trust; implementation `17c2644`
- Source evidence: Phase 1 design §5, implementation plan Task 5, Accepted ADR-0001/0002
- Affected families/roles/routes/tables: Family, Income, Expense, RecurringTransaction, IdempotencyRecord and AuditEvent; no route or role behavior changed
- RED command: `npm test -- --runInBand src/tests/phase1SchemaContract.test.ts`
- RED failure summary: 2/2 tests failed because `baseCurrency`/version/currency/origin fields, IdempotencyRecord/AuditEvent and both ordered migration files did not exist. A second focused RED proved AuditEvent's mutation relation was incorrectly configured as cascade rather than NO ACTION.
- GREEN commit and scope: `17c2644`; additive schema and two ordered migrations; historical base currency/currency/version backfill, positive version/interval and three-character currency checks, scoped idempotency unique index, response/hash/completion checks, append-oriented AuditEvent relationships, and opt-in integration discovery for all `*.integration.test.ts`
- REFACTOR summary: Prisma schema was formatted; operations/audit do not receive cacheVersion triggers, and origin fields remain nullable for old-route compatibility
- Regression commands/results: schema contract PASS 1 suite / 2 tests; integration-discovery config PASS 1 suite / 5 tests; combined focused PASS 4 suites / 19 tests; `npm run build` PASS; Prisma format/validate/generate PASS using matching local 5.22 engines
- Security negative matrix: family foreign keys and family-scoped unique key are persisted. Unauthenticated/non-member/viewer behavior remains service/route evidence; no route was connected in this task
- Database/migration evidence: PASS-REAL on dedicated PostgreSQL 18.1 `homefinance_phase1_test` at localhost:5433. A fresh database applied all 6 migrations via `prisma migrate deploy`; `npm run test:integration` then passed 2 suites / 20 tests, including scoped uniqueness, defaults, stale version arbitration, cacheVersion and rollback regressions
- Frontend/E2E evidence: NOT_RUN unless task is design-only
- Release environment/time: local Windows, PostgreSQL 18.1, fresh dedicated non-production database, 2026-08-28; not staging/released
- Observed result: the database rejects a second identical scope row with P2002, legacy-compatible writes receive CNY/version=1 defaults, and a stale expected version updates zero rows
- Rollback rehearsal: migration deploy was exercised from an empty database; destructive down migration was not run. Rollback remains forward-fix or restore while preserving operation/audit facts
- ADR/project-memory/audit/Graphify updates: ADR-0001/0002 Accepted; project memory and tracker record `17c2644`; Graphify semantic refresh is part of the following governance commit
- Remaining risks: AI Balance mutation, populated upgrade/restore, staging/release and global coverage remain open. Income/Expense update/delete, Import, Recurring and supported AI Income/Expense confirmation now consume database-arbitrated contracts with separate evidence; P1-B-04 retains its historical concurrency caveat.
