# P1-E-05 Evidence

- Task ID: P1-E-05
- Evidence status: REGRESSION_VERIFIED / AT_RISK
- Baseline commit: `cdfcb67`
- Branch/commit: `codex/phase1-ledger-trust@c769a78` (`d945e65` and `c769a78` frontend/history slices)
- Source evidence: `frontend/src/pages/AIPage.tsx`, `frontend/src/services/aiService.ts`, `frontend/src/pages/AIPage.test.tsx`, `backend/src/routes/ai.ts`, `backend/src/tests/aiProposalRoutes.phase1.integration.test.ts`
- Affected families/roles/routes/tables: selected authenticated family; member/admin confirmation UI; server-owned proposal metadata from `/ai/history`; confirmation endpoint with proposalId/version/hash and stable Idempotency-Key
- RED command: `cd frontend; npm test -- --run src/pages/AIPage.test.tsx src/services/aiService.test.ts`
- RED failure summary: refresh history discarded proposal identity, confirmation could lose `proposalItemId`, and executed proposals had no visible completion state or confirmation guard.
- GREEN scope: `d945e65` restores pending chat/OCR proposals from server history and preserves proposal item IDs; confirmation prioritizes server-owned `proposalItems` over stale local action data. `c769a78` exposes persisted executed results in the history contract and renders `已完成 1 笔记账` without a confirmation button.
- Regression commands/results: frontend focused AI page/service tests PASS, 2 files / 6 tests; backend route/service regression PASS, 3 suites / 52 tests; real PostgreSQL history/confirmation route suite PASS, 17 tests. Tests cover failed confirmation retry, pending-click suppression, pending refresh recovery, OCR refresh recovery, executed refresh state, and no duplicate confirmation.
- Security negative matrix: the UI does not replace server validation; proposalId/version/hash and final actions are sent to the backend, where authorization, canonical validation and idempotency are enforced. Viewer, tamper, expiry, replay and concurrency negative evidence is recorded under P1-E-06. Browser E2E and multi-tab race behavior remain NOT_RUN.
- Database/migration evidence: no schema change in this frontend slice; server result persistence is verified by the PostgreSQL route suite.
- Frontend/E2E evidence: PASS-MOCK via Vitest/React Testing Library; browser E2E, reload with real browser storage, multi-tab conflict and accessibility automation remain NOT_RUN.
- Release environment/time: local Windows, 2026-09-01; release observation NOT_RUN
- Observed result: proposal editing/confirmation can resume after refresh for pending records, and executed records remain visibly complete after refresh. Cancellation semantics are not implemented as a separate server lifecycle transition.
- Rollback rehearsal: revert UI/service changes only together with the matching backend contract; preserve server-owned proposal facts. Do not restore a client-owned raw-action confirmation flow as an operational fallback.
- ADR/project-memory/audit/Graphify updates: tracker, project memory, ADR-0004 and frontend audit analysis are updated; semantic Graphify refresh remains pending.
- Remaining risks: explicit cancellation, browser E2E, multi-tab conflict UX, legacy endpoint removal, external dependency behavior and release observation.
