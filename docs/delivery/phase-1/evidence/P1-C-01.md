# P1-C-01 Evidence

- Task ID: P1-C-01
- Evidence status: PASS-MOCK for the HTTP/parser contract; no database or external service is required for this slice
- Baseline commit: `a21d7a1` on `codex/phase1-ledger-trust`
- Branch/commit: `codex/phase1-ledger-trust@182c9aa`
- Source evidence: `backend/src/services/importLimits.ts`, `backend/src/services/importService.ts`, `backend/src/services/importService.phase1.test.ts`, `backend/src/routes/import.ts`, `backend/src/routes/import.phase1.test.ts`
- Affected families/roles/routes/tables: `POST /api/families/:familyId/import/csv`; authenticated family members with `admin` or `member` role; no schema/table change
- RED command: `npm test -- --runInBand src/routes/import.phase1.test.ts`
- RED failure summary: all three new contracts returned 200 instead of 413: oversized bytes, more than the row limit, and an oversized field.
- GREEN commit and scope: shared `IMPORT_LIMITS` defines 10 MiB, 10,000 data rows, and 512 characters per field. Multer rejects oversized files before parser execution; parser and route reject oversized row/field results with stable `{ error: 'IMPORT_LIMIT_EXCEEDED', limit }` responses. Existing auth and family authorization middleware remains before multipart parsing.
- REFACTOR summary: byte/row/field policy and typed `ImportLimitError` are centralized in `importLimits.ts`; both direct parser calls and HTTP route results use the same limit contract. The route upload adapter uses Express `Request`/`Response`/`NextFunction` types.
- Regression commands/results: focused import tests PASS, 3 suites / 18 tests; `npm run build` PASS; `npm run test:integration` PASS, 5 suites / 33 tests; full default behavior tests PASS, 39 suites / 328 tests, with coverage at statements 69.68%, branches 51.69%, functions 69.68%, lines 70.24%. The required command still exits non-zero only because the unchanged global branch threshold requires 60% and branches are 51.69%.
- Security negative matrix: existing real PostgreSQL role/method matrix still proves unauthenticated 401 and viewer/unknown-role/non-member 403 before family mutation handlers. This slice adds no new family data access before authorization; an oversized unauthorized upload remains rejected by the authorization middleware before Multer parsing.
- Database/migration evidence: no migration and no persistence behavior change; local PostgreSQL integration remains 5 suites / 33 tests PASS.
- Frontend/E2E evidence: no frontend code changed; frontend lint/build remain historical PASS with existing warnings and oversized chunk; browser E2E NOT_RUN.
- Release environment/time: local Windows, 2026-09-01; no staging/release observation.
- Observed result: P1-C-01 is REFACTORED / AT_RISK. Resource limits and stable 413 handling are implemented and regression-covered, while the global coverage gate and the subsequent server-owned ImportBatch/ImportRow workflow remain open.
- Rollback rehearsal: not run. Revert the import upload/parser adapter as one entry point; no schema or persisted import facts are removed.
- ADR/project-memory/audit/Graphify updates: ADR-0003 remains applicable. Tracker and project memory record the limit contract. Semantic Graphify refresh remains pending because the available AST-only updater would overwrite the reviewed semantic graph.
- Remaining risks: P1-C-02~06 ImportBatch/ImportRow persistence, server-owned preview hash, atomic confirm/replay/concurrency, client tamper resistance, global branch coverage, external infrastructure, E2E, staging/release, and semantic Graphify tooling.
